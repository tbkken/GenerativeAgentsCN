# 日常作息地图源素材

四层图片、预览图和原始参考图统一在此目录维护。旧 frontend/static/assets/experiment 下的相同 PNG 副本已移除；这些图片是作者源素材，不是当前运行时默认地图。

本机 daily-routine-map-manifest.json 如存在，仅记录当时编辑器草稿的 ID、素材哈希和定位信息，不是 `.gaexp` Manifest，也不能直接恢复实验。本次去重只将其中的素材路径更新为此处的现存文件，原 ID、状态和哈希保持不变。

正式案例、实验和 Run 仍需各自保存完整物理副本。
