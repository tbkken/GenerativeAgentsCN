# 许知行整理书桌：状态外观实施与页面验收

日期：2026-09-06。配置与运行验收由主 Agent 仅通过浏览器页面完成；源码实现与代码测试由用户指定的子 Agent 负责。

## 独立资源

- Map：知行自习室，`4d3ee8bd-5cf3-4ad9-aa7d-59b71722893f`，12×12 格。
- 人群：许知行整理书桌 · 独立单人，`6c56128d-f949-4aad-9338-709004d891e9`。
- Agent：许知行，页面可见 key `agent-mtppri3c-kty6yk`；公共 UUID 未在当前页面显示，未猜测。
- Brain Skill：`desk-tidying-v1-brain`，`25eba9c6-2566-4107-8800-48cf59780b06`。独立创建，无子 Skill，仅 `world-perceive` 与 `world-act`。没有把“已整理”枚举重复硬写进 Brain，而是要求从合法观察获得状态名。
- 实验：`5ac3792c-6df5-4329-868a-538bf9e6cc7b`，已封存。
- 首 Run：`a961f3e7-d80e-4043-adae-f0ddba278839`。

没有修改溪谷大学的作者资源或运行配置。素材原图与切片全部通过页面上传、创建和保存。

## 场景配置

房间底图绑定 World。书桌范围 `(3,3,6,3)`，材料对象范围 `(5,4,2,2)`，角色初始站位 `(6,6)`。书桌与材料为 Arena 下同级 Game Object，没有增加第五层。

地图预检要求初始可走位置有完整四层地址；因此在 Arena 下另建无素材的“桌前站位” Game Object，覆盖 `(5,6,2,1)`。它描述可站立地板，不改变桌子实体。页面两点检查 `(6,6) → (6,5)` 拒绝“终点位于阻挡格”，证明书桌/材料实体通路阻挡保持。

材料对象默认显示散放素材，初始状态 `{}`。用户在“显示素材”下点击“＋ 增加状态”，输入“已整理”，选择码齐素材，沿用原保存操作；没有绑定被动 Skill。

## 首次真实 LLM 运行

4 步全部完成，实际耗时 2 分 58 秒，8 次逻辑调用 / 8 次物理尝试，0 重试。浏览器已核对：

| Step | 虚拟时间 | 实际行为 |
| --- | --- | --- |
| 1 | 09:00 | ACT 整理材料，图片仍为散放 |
| 2 | 09:01 | SET_OBJECT_STATE，GAME_OBJECT_STATE_CHANGED 的 after 为 `{"state":"已整理"}`，图片切换码齐 |
| 3 | 09:02 | ACT 检查整理结果 |
| 4 | 09:03 | ACT 阅读材料 |

Step 3、4 检查点均 VALID，各 11 文件 / 15 KB。证明真实 Agent → MCP → 世界提交 → 回放图片链路，而非前端按 Step 换图。

## 首次浏览器暴露的问题与修复

1. 中文新建人群自动 key 冲突：自动生成稳定唯一后缀，已页面回归通过。
2. 作者库废弃 is_builtin 非空列导致创建500：授权备份后无损移除废弃列，原4 Agent与1人群有效字段/索引/外键保持；备份 `var/backups/before-remove-is-builtin-20260906-191210.sqlite`。
3. 新服务旧脚本：ga_studio Web 补 no-cache 并更新资源版本，状态入口已页面可见。
4. 地图编辑器按裁片40×40占格绘制，忽略节点12×12范围：与 Replay统一按节点范围绘制，页面比例恢复正常。
5. 首次下一步只从0到1、分母4/2来回变化：Player首帧自动定位并独占已提交帧，分母使用计划总步数；页面已确认第一次下一步直接 Step2/4。
6. 事件默认整段JSON与动作气泡溢出：自然语言摘要+展开事实详情，气泡限长换行；页面已确认。
7. 质量定位按钮被转义成文字：修正安全模板；页面已能点击并定位Step3。
8. 跨Step的常规观察被误报无进展：质量诊断改为同轮相同输入、相同结果且无成功写入/动作介入才提示。旧 Run 报告作为修复前证据保留，不能手工消除告警；第二Run待页面验证。
9. 时间轴两端硬编码06:00：修正为包内开始时间与计划最后Step时间，使用实验时区；待最后页面回归。
10. 轮询重建质量面板导致折叠：同Run内容未变不重建，内容变化保留展开状态；待最后页面回归。

代码测试仅作辅助，第二 Run 与剩余 UI 最终验收结果见下文。


## 第二轮页面回归（已完成）

第二 Run：`a329bb11-a6ed-40c1-a622-66083c313560`，经同一已封存实验页面“执行下一次仿真”创建。没有手改首 Run 报告或事实。

已验证：运行中时间轴首尾显示 `09/07 09:00 → 09/07 09:03`，不随当前完成步数缩短；切回旧 Run 展开质量详情，等待 12 秒并经历多次自动刷新，仍保持展开，定位按钮可用。第二 Run 已完成 4/4，实际耗时 2 分 33 秒。Step3 检查已整理材料、Step4 阅读笔记本均再次经浏览器确认。最终报告没有重复读取误报告警。


### 质量显示语义修正

第二 Run 报告的模型评估为 `SKIPPED`，没有实际执行失败。原通用映射将“未执行业务评估且基础检查无告警”显示为“评估不可用”，已修正：页面依据报告真实的 `evaluator.status` 与告警列表解释为“基础诊断未发现告警；本次未执行业务评估”，不能据此宣称业务评估通过。新报告生成使用 `NOT_EVALUATED`，与真正的评估异常区分。

旧报告没有手改，未追加任何模型评估调用。本例业务目标由两次 Run 的实际动作和对象状态事实人工验收。最后页面文案已由主 Agent 刷新确认：“行为质量：未评估”“基础诊断未发现告警；本次未执行业务评估。（不改变运行完成状态）”。第二轮模型统计为 8 次逻辑调用 / 8 次物理尝试，0 重试。

### 自动化辅助检查

状态配置与缺失分支校验、感知与合法动作、包内语义索引、跨轮/同轮质量边界、可移植包与模块边界联合回归：48 项通过。前端覆盖精确状态匹配、回放倒退、初始化首帧、节点显示范围、摘要/展开原始事实、质量定位按钮与跳过评估映射等。自动化不替代上述浏览器证据。


## 最终结论

本案例的管理台构建与两次真实 LLM 短 Run 均已完成浏览器验收：独立资源创建、状态素材保存、合法近距状态修改、真实状态事实驱动画面、步骤切换与事件解释均可使用。新增状态外观不要求默认场景增加配置步骤。源码由指定子 Agent 实施；主 Agent 没有修改源码，所有应用配置与最终验证均通过浏览器。

本次没有声称完成需求矩阵中所有极端场景（如跨主机离线迁移、损坏纹理、多人同一步竞争、暂停恢复）的浏览器验收；这些仍应按各自矩阵单独回归。书中第一案例的单人整理目标已通过真实事实验证。
