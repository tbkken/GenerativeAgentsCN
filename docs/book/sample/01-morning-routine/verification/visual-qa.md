# 视觉验收：回放已验证，出版图片待补齐

日期：2026-09-09。案例 1 按“睡醒→书房整理资料→备餐和早餐→开始工作”设计。运行结构校验、初始画面与完整行为验收分别记录，当前运行记录见 `../settings/verification-record.md`。

## 旧方案的确定问题与处理

1. 首次方案的三张写实地图图片确实物理复制自旧 `desk-tidying-v1`，没有重新统一美术设计。它们已移至 `legacy-assets/map/`，并在 `legacy-assets/README.md` 记录来源和哈希，不再作为新地图的活跃素材。
2. 旧房间、书桌是写实俯视插画，角色是 96×128 的 3 列×4 行像素图集，风格、轮廓与光照不一致。
3. 旧图的 1254px 源尺寸不能对应 32px Tile 的整数网格；旧 1×1 整图切片与约 6×4 Tile 的桌面配方，也没有证明角色、家具和站位的比例正确。
4. 旧方案只有单房间整理桌面的短活动，不支持新场景需要的跨房间行动。这次改为住宅布局后，必须重新检查所有移动与操作站位。

新素材与地图遵守 `../map/visual-contract.md`。案例图集仍需与新地图一起验收，不能因为其单帧为 32×32 就自动认定人物比例正确。

## 验收记录

| 检查项 | 当前状态 | 通过所需证据 |
| --- | --- | --- |
| 旧素材不再用于活跃地图 | 当前浏览器配置使用自制完整住宅和两桌面源图；历史 Room Builder 仍保存在 map/assets | 最终包资源清单不把历史素材作为活跃地图依赖 |
| 背景和初始人物显示 | 已在进度便签版 Run 初始画面确认 | 三张源图均 1448×1086，地图 32×24、Tile 32，人物帧 32×32、显示大小 3 格；完整背景和散放桌面可见 |
| 四处操作站位的人物比例 | 3 格人物显示与跨房间活动已确认；出版近景待补 | 床边、书桌前、厨房台前、早餐桌旁的近景及遮挡检查 |
| 房间间移动与碰撞 | 当前 Run 完整动线已确认 | Step 4–5、12–14、20–21、25–26 的真实 MOVE 与回放；全地图碰撞穷举不在本次验收范围 |
| 默认缩放与放大清晰度 | 初始全景已查看；记录具体缩放值的出版验收待补 | 记录画布大小与缩放值的截图 |
| 动作事实与画面一致 | 桌面状态变化及返程保留已确认 | Step 9 的 `SET_OBJECT_STATE` 与切图一致，Step 26/32 仍为 `tidy`；ACT 文本不代替移动或对象状态事实 |

初始住宅与人物同屏、3 格人物显示及卧室→书房→厨房→餐区→书房的实际生活动线已确认。第 9 步已核对真实对象状态 payload，桌面由散纸切成整齐本册，人物仍在 `[23,7]`，返程桌面仍为 `tidy`；32 步行为已完成，浏览器导出材料已校验归档。出版使用的操作近景、缩放值和遮挡检查仍待补齐，不能将本目录描述为已通过全部出版画面验收。

