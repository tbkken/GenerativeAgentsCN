# P01–P03 修复与浏览器回归

日期：2026-09-09。用户明确授权启动三个子 Agent，分别修复状态来源混用、历史导出来源污染及恢复后动作摘要时区问题；P04–P09 不在本轮实施范围。

## 验证对象与边界

- 原参考实验：`b1c550ec-1599-4cb8-a4c8-7f303c7344df`，原 Run `4f5713c4-7b76-4afe-8f3a-b4bd36ee2a28`，32/32 完成。原始文本日志和归档产物不重写。
- 主 Agent 已通过浏览器复制独立回归实验：`56ff8cc0-ecae-4691-bf7d-ab4910f98b79`，名称“案例1：P01–P03 修复回归（6步）”。继承案例自己的地图、人物、模型和 Brain，起点 `[9,5]`，只将观察窗口设为 6 步。
- 该短实验用于验证暂停恢复和导出边界，不验证完整的 32 步晨间故事，也不是正常实验的强制前置试跑。
- 工程子 Agent 只改代码与针对性测试；主 Agent 的配置、启动、暂停、恢复和导出均从 UI 操作。服务统一使用已授权的 `restart-web.bat` 重启。

## 修复前浏览器证据

- 原参考 Run 的结果页顶部显示“已完成”，该字段混用了 Run 与实验生命周期；正确分离后实验应显示“已封存”，Run 选择器仍显示自己的“已完成”。
- 原参考 Run 有 4 个制品，包括 `result-bundle-step-000021.zip` 与 `result-bundle-step-000032-29063611.zip`。原列表只有文件名、大小、格式与哈希，没有清楚展示可信的生成来源；缺少生成记录的历史文件不能直接套用当前 Step 32。
- 恢复后摘要时区的原故障见 [最终验证记录](../settings/verification-record.md)。旧 Step 11 日志是历史文本；刷新旧日志不能作为新修复的验收。

## 实现与自动化检查

- P01：实验详情和列表只以 DRAFT/SEALED 表示生命周期；Run 状态来自实时文件，列表的运行类筛选明确为“最近仿真”。所选 Run 与异步历史列表响应按当前身份隔离。
- P02：`artifact-metadata/` 保存与文件名、SHA256、大小、Run 身份关联的来源。导出冻结提交边界，排除未提交帧和 Attempt 可变工作记忆；持锁后再次检查待导出的检查点仍存在，避免清理竞态生成空 ZIP。历史缺记录或哈希不符不回填来源；正式包入口不在本轮范围。
- P03：Action 摘要按注入的仿真时区渲染，保存为带偏移、保留精度的 ISO 时间；不更改绝对时刻、时长或 Scheduler。
- 合并检查：P01/P02/P03 三个新增 Python 测试文件 16/16，全体前端测试 35/35，共 51 项通过。各子 Agent 还分别完成相关协议、事件同步、引擎隔离及模块边界检查；这些有重叠，不再与 51 相加。
- P02 交叉审查发现的可变记忆混入及检查点清理竞态已补测试并再次只读复核通过。

## 浏览器运行记录

- 新 Run：`0cd75b46-f3e2-4068-8c40-59cfdee5e753`。通过 UI 预检，0 阻断、0 警告后封存启动；顶部已封存，Run 下拉依次显示排队中、运行中。
- Attempt 01：`2fadf608-f95c-4616-948e-70fb8432121b`，提交 Step 1–2 后按 UI 请求安全暂停。顶部仍为已封存，Run 为 PAUSED 2/6。
- 经 `restart-web.bat` 重启加载最终 P02 修复后，Run 仍停在 Step 2。通过 UI 导出 `result-bundle-step-000002-3e67e286.zip` 与对应质量报告，列表显示 Step 2/6、部分结果、取材时已暂停、带 Asia/Shanghai 的生成时间。
- 通过 UI 确认从 Step 3 继续，同一 Run 创建 Attempt 02：`1df1a004-f006-440d-9ede-d0a028b86742`。Run 恢复 RUNNING，顶部仍为已封存。
- P03 浏览器证据：Attempt 02 的首个恢复 Step 标题为 `20260909-07:06:00`，上一动作摘要为 `20260909-07:03~20260909-07:06`，未再显示前一天 UTC 墙钟；前两步没有重新提交。
- 原参考 Run 的 4 条历史制品均显示来源未知，而不是套用当前 32/32；原文件保持原名与哈希。来源未知表示没有可信的生成边界记录，不表示文件损坏。
- 在 1176×912 的浏览器截图中发现已封存徽标被挤成竖排；最小样式修正后刷新复验，徽标横排、标题适当省略，按钮未溢出。该样式修正另通过 4 项既有 P01 前端检查，不与上面的合并检查累计计数。
- Run 最终 COMPLETED 6/6，Attempt 01 提交 1–2，Attempt 02 从 3 继续至 6。完成后顶部保持已封存，Run 下拉显示已完成；参考 Run 和新回归 Run 的生命周期都未被执行状态覆盖。
- 完成后 UI 再次生成 `result-bundle-step-000006-26e66786.zip` 及独立质量报告，显示 Step 6/6、最终结果、取材时已完成、带 Asia/Shanghai 的生成时间；旧 Step 2 ZIP 与报告仍显示 Step 2/6、部分结果、取材时已暂停及原生成时间。
- 列表“最近仿真：已完成”返回本次 6 步回归和原 32 步参考实验，两者均为已封存，计数与结果均为 2。“已封存”与最近 Run 状态作为不同筛选维度。同实验多个历史 Run 的异步切换由自动化测试覆盖，未额外创建空 Run 冒充浏览器复验。
- 完整质量报告保留 3 项观察：Step 1 首次空记忆、Step 4 和 5 导航 `[23,7]` 超出视野。P06 未修改，不能把三个工程修复的验收通过写成行为质量零告警。
- 四个 UI 导出和四份来源记录已原样归档到 [P01–P03 导出记录](p01-p03-exports/README.md)，复制后哈希与源文件相同。原 32 步参考材料继续保留在 `exports/`。

## 中途 ZIP 基线

- 文件：`result-bundle-step-000002-3e67e286.zip`，5,684,858 字节。
- SHA256：`36f0e6739fd5dfbef86cd3a859659b2021ddb82df58044b6cebeb63eecf13a1b`。
- 来源：本 Run / 本实验 / Attempt 01；PAUSED，2/6，partial=true；生成于 `2026-09-09T06:51:31.499009Z`。
- ZIP CRC 通过，只含 Step 1、2 帧，无 Attempt 可变工作存储。Run 完成 6/6 并生成新导出后再次核对，此文件大小、完整 SHA256 和固定来源全部保持不变。

## 最终 ZIP 核验

- 文件：`result-bundle-step-000006-26e66786.zip`，5,753,459 字节。
- SHA256：`6b17db5ab4e163c78ebe717abbe3c553fae7f86fc2b52251067818f66044a438`。
- 来源与包内状态为 COMPLETED、6/6、partial=false；6 个连续帧，两个 Attempt 分别提交 2 和 4 步；CRC 通过。
- Step 2、Step 6 的独立质量报告分别与各自 ZIP 内报告逐字节一致。固定来源记录与这四个文件的身份、大小及哈希关联，详细原值见归档的 `provenance/`。
- 三项修复已完成。普通 ZIP 仍是审计结果，不是正式 `.garun` 或新的完整 32 步教材参考 Run。
