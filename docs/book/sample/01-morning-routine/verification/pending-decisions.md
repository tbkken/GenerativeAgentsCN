# 案例 1：问题清单与处理状态

更新：2026-09-09。以下保留发现时的证据与建议。用户已明确授权三个子 Agent 修复 **P01、P02、P03**，三项代码修复、合并检查及独立 6 步浏览器回归已完成，见 [P01–P03 回归记录](p01-p03-regression.md)。**P04–P09 仍待决定**，参考 Brain 保持不变。

证据基准：实验 `b1c550ec-1599-4cb8-a4c8-7f303c7344df`，Run `4f5713c4-7b76-4afe-8f3a-b4bd36ee2a28`，32/32 完成；[逐步验证记录](../settings/verification-record.md)、[原样导出及哈希](exports/README.md)。两次 Attempt 分别提交 Step 1–10、11–32。

## 清单（P01–P03 已处理，P04–P09 待决策）

| 编号 | 类别／建议顺序 | 实际发现与影响 | 建议处理路径 | 验收条件 |
| --- | --- | --- | --- | --- |
| P01 | 已修复并复验 | 同一 Run 恢复后，Run 面板已为 RUNNING，顶部仍为旧 PAUSED；实验字段被最新 Run 状态覆盖。 | 实验名旁明确显示 DRAFT/SEALED；所选 Run 状态独立来自实时文件；列表生命周期与最近仿真状态分开筛选，并保护异步 Run 选择。 | 6 步回归的排队、运行、暂停、恢复和完成期间，实验状态始终已封存；1176×912 下徽标横排，按钮无溢出。历史 Run 切换与异步竞争由自动化覆盖。 |
| P02 | 已修复并复验 | 历史导出的 `source_step`、`partial` 被当前 Run 状态污染，可能把中途 ZIP 标成最终结果。 | 固定记录取材提交边界/状态及生成时间并关联文件哈希；缺记录显示未知。ZIP 排除未提交帧和 Attempt 可变工作记忆，生成时不覆盖旧导出。 | Step 2 导出在 Run 完成 6/6 后仍为部分结果、PAUSED 来源，大小及完整哈希不变；新 Step 6 导出独立存在。来源记录和四个 UI 产物已归档。 |
| P03 | 已修复并复验 | 恢复后的 Step 11 标题为 07:30，上一动作摘要却为前日 23:27–23:30。 | 摘要按注入的仿真时区显示，序列化保留偏移和精度，不改变绝对时刻与时长。 | 新回归从 Step 3 恢复，07:06 标题与 07:03–07:06 动作摘要一致；跨日期、不同时区和连续恢复测试通过。原历史文本日志保留，不重写。 |
| P04 | 已确认显示 bug；清理占用原因待复现／随后 | Step 13 旧检查点清理出现一次可恢复 PermissionError。后端发 WARNING，但 UI 在整行中搜到 `error=PermissionError`，误标 ERROR。新检查点及 LATEST 正常，后来清理完成，最终只保留 Step 31、32。 | 代码：优先读取结构化日志级别；文本解析限制在级别字段。保留安全清理和有界重试，长期清理失败再提示维护风险。本次不足以定位哪个进程占用文件。 | 临时占用不被展示为检查点提交失败，释放后清理成功；UI 级别与后端一致。若再次发生持续占用，再单独定位，不能先宣称数据损坏。 |
| P05 | 估算能力与文案不足／随后 | 创建页预计 32–96 次调用、约 2–16 分钟，实际为 108 次逻辑调用／108 次物理尝试。页面总耗时 37 分 52 秒含约 4 分钟人工暂停；两次 Attempt 合计约 33 分 52 秒，仍超过估算。估算实际使用固定 Agent×Step 常数，文案却称考虑了 Brain 调用链。 | 代码与 UI：用实际调用链、上下文、模型吞吐和重试校准区间；缺少数据时标为粗估。显示暂停时间和执行时间的口径。 | 相同模型与案例的后续估算可解释实际结果；逻辑与物理次数分开。Token 数据缺失时显示未知，不能据本次判断 Token 估算误差。 |
| P06 | Brain/SOP 改进，可经界面编辑／随后 | Step 4、12、20 共 3 次远端导航超视野被拒绝，Step 4 另有 1 次 MOVE 猜到阻挡格；均同轮修正后成功。硬视野和碰撞拒绝正常，但学员会多付出调用和等待。 | 通过 UI 修改 Brain：远端目的地经已知、经感知及可达性验证的视野内途经点推进；区分最终站位与当前分段目标。不扩大视野来消除诊断。 | 在新的独立实验中验证完整生活路线，确认不再猜门洞、误把 `next_coord` 当终点或提前执行目的地 ACT。保留旧精确 Skill 与旧 Run 的对应关系。 |
| P07 | 质量解释体验，可选 | Step 1 第一次检索自己的便签为空，符合全新 Agent 的初始状态，却容易被理解成记忆坏了。本次只有 1 条 EMPTY_MEMORY_RETRIEVAL。 | 代码／UI：在详情中解释初始空记忆、已恢复继续等上下文，可区分信息提示与需要处理的告警；保留可追踪记录。 | 学员能看出“首次无记录”和“已有记忆却检索不到”的区别；不能全局屏蔽空检索，也不能把其他 Agent 的记忆补给当前 Agent。 |
| P08 | 行为文字精度，可经界面编辑／可选 | Step 24 在餐区 `[22,17]` 收拢餐具，描述出现“将餐具放回厨房方向”。没有真实 MOVE 回厨房，文字容易被当成已送回。当前资料已按事实限定为餐区收拢。 | 通过 UI 收紧 Brain 的 ACT 描述，只叙述当前位置内完成的活动。若案例确实要演示送回厨房，则增加真实 MOVE 和后续 ACT，再评估观察窗口。 | 文字、位置和事件一致；不以 ACT 代替移动，不把餐具状态或位置当作未提交的世界事实。 |
| P09 | 导出入口与交付边界，可选 | 本次“导出结果”得到普通 ZIP。32 帧、五张图片、嵌入实验完整性和 5 项质量报告均通过校验，但它没有 Run 根完整性清单，不能当成正式 `.gaexp` / `.garun` 或一键恢复包。现有正式封存能力的入口易用性尚需体验核查。 | 先核查既有正式包入口；UI 区分“结果审计 ZIP”“实验包”“运行包”及用途，必要时接通既有正式封存能力。不要只改扩展名或把运行中普通导出当封存。 | 正式包脱离作者数据库和原目录可校验、打开；暂停包续跑保持 Run 身份和已提交边界。普通 ZIP 明确保留审计用途。 |

P01 涉及 `AGENTS.md` 中既有的实验生命周期与事实来源边界，其状态分离方案现已获得用户授权。该授权范围为 P01–P03；P06、P08 虽可通过作者界面修改，本轮仍不实施。确定的实现缺陷需要工程修改后回到浏览器验收。

## 只读诊断定位

- P01：`generative_agents/web/portable_api.py` 的实验详情与 `submit_directory`，以及 `generative_agents/web/static/console-api.js` 的顶部状态投影。目录索引与当前 Run 文件读取并非同一更新时点。
- P02：`generative_agents/web/portable_api.py` 的制品列表组装，对历史制品使用当前 `committed_step` 与 Run 状态。
- P03：`generative_agents/modules/memory/action.py` 的 `Action.from_dict` 与 `Action.abstract`，恢复转换和摘要格式化的时区口径不同。
- P04：`generative_agents/runtime/checkpoint.py` 的旧检查点清理发 WARNING；`generative_agents/web/static/console-api.js` 的整行日志级别关键词匹配会命中 `error=`。
- P05：`generative_agents/web/portable_api.py` 的运行成本估算。
- P09：`generative_agents/web/portable_api.py` 的普通结果导出，与 `generative_agents/ga_runtime/service.py` 的 `RunService.seal` 具有不同合同。尚未因本次普通 ZIP 验证而声称正式入口已验收。

## 已处理项目，避免再次当成待修 bug

- 人物过小、缺少视觉素材，以及错误站位、坐标/地址用法、缺少跨步进度：当前参考案例已通过 UI 配置和 Brain 修订改善。地图有整屋背景和桌面两态，头像、行走图及 3 格显示已确认；残余导航纠错列为 P06。
- `world-perceive.events` 旧地址跟随新坐标：工程修复后 51 项相关检查通过，浏览器已复验。
- 回放数据增长后点击角色卡在等待事实窗口：工程修复后 29 项检查通过，浏览器已复验。
- 暂停恢复后最终质量报告只含最新 Attempt：工程修复后 57 项检查通过；UI、独立报告与新 ZIP 均包含全部 5 项。

以上修复是 P01–P03 任务开始前已经完成的工作。原来已封存的 32 步 Run 事实、精确 Brain 和归档产物继续保留；新回归使用独立实验，不把短回归宣称为新的完整晨间行为验收。

## 教材交付仍需补齐的部分

- 出版图片：完整回放和桌面切换已观察；四处操作站位近景、缩放值、脚底锚点细节和遮挡检查尚未形成完整出版图组，见 [视觉验收](visual-qa.md)。这是素材验收待办，不是已发现的画面 bug。
- 复现资料入口：当前以中文浏览器录入清单为准，旧 JSON 标为历史参考。是否将历史文件统一迁出主目录、提供经正式协议验收的实验包，可与 P09 一并决定；不能把旧 JSON 宣称为当前可导入配置。

## 本轮资料核对与修正

主要素材、精确 Brain、32 步事实、报告和 ZIP 已在对应目录。核对也发现此前并非每处说明都已同步，本轮补正了：

- 人物录入说明的旧 `[3,4]`、旧地址、视野 4，改为 `[9,5]`、当前四层地址、视野 10，并补齐当前人物字段；禁止跟随旧 JSON 录入。
- `settings/results.json` 和说明中的 Payload 关闭，改为本次实际开启；总清单补齐视野/注意力、检查点间隔 1、保留 2、投影间隔 1。`models.json` 的旧示例模型与端口已改为归档中的实际非凭据字段，凭据环境变量仍使用教学示例。
- 事件同步修复仍写“待浏览器回归”、人物和视觉表仍写“待实际回放”的旧状态，更新为已验证范围，同时保留出版近景等未验项目。
- 修正 Skill 说明的实际文件链接，并同步总纲中的对象状态、暂停恢复与跨 Attempt 质量验收范围。

后续操作经验已写入仓库 `AGENTS.md` 第 13 节；具体实例数据仍保留在案例资料中，不变成系统默认配置。
