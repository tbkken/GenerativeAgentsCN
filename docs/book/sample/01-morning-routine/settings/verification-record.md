# 最终验证记录（2026-09-09）

以下 ID 只用于验证记录，不是学员创建新实例时要填写的配置。

## 历史问题记录

- 实验：`30d3a97b-1b3f-434e-b6a2-3bdf70c7711d`，已 SEALED。
- Run：`191d8d47-c218-450f-b3c0-540875d64f16`，在第 3/32 步暂停。
- 已观察到：起身、穿衣、整理床铺三条连续生活 ACT。
- 问题：人物相对床和家具偏小。

## 第二次运行：行为验收未通过

- 实验：`e8406308-0c5d-45ea-9c60-59a526e108af`，已 SEALED。
- Run：`3ad4ff95-bccc-4dc9-b99d-c6e7a99807c6`，32 步×3 分钟，地图显示大小设为 3 格。
- 状态：已安全暂停在 26/32 步；执行状态 PAUSED，行为验收未通过，出现重复穿衣和晨间流程重启。
- 诊断：Step 6 的真实坐标为 `[11,5]`，不是出生点 `[9,5]`。旧地址跟随新坐标涉及 `world-perceive.events` 的提交顺序缺陷；此外存在以阻挡对象地址寻路、把 `next_coord` 当最终目标、缺少持久跨步生活进度三项问题。详见 `../verification/README.md`。

## 当前验证对象：进度便签版

- 实验：`b1c550ec-1599-4cb8-a4c8-7f303c7344df`，名称“案例1：陈明远的晨间生活（进度便签）”，已 SEALED。
- Run：`4f5713c4-7b76-4afe-8f3a-b4bd36ee2a28`，状态 `COMPLETED`，已完成 32/32 步。曾在 10/32 步安全暂停，用于修复回放点击角色后未拉取当前 Step 的前端缓存问题；修复通过 29 项测试并重启后，同一 Run 从 Step 10 检查点续跑，新 Attempt 从 Step 11 开始，前序未重跑。晨间行为已走通，跨 Attempt 质量汇总及最终浏览器导出也已复验。
- 参数：2026-09-09 07:00 Asia/Shanghai，32 步×3 分钟，随机种子 42，人物显示 3 格。
- 参数补记：检查点间隔 1 步、保留 2 个；Agent Step 投影间隔 1 步、模型 Payload 记录开启。四项均已从浏览器配置与嵌入实验核对：前两项在 `experiment/runtime/simulation.json`，后两项在 `experiment/evaluation/evaluators.json.results`。Agent 32 岁，视野 10、注意力 6，出生点 `[9,5]`；完整人物文字已按嵌入实验同步到 `../agents/陈明远/agent-definition.txt`。
- 模型补记：嵌入实验的聊天配置为 vllm / `Qwen3.8-27B-UD-Q4_K_XL` / `http://127.0.0.1:8888/v1`，temperature 0.2、thinking=false、timeout 90 秒、retry_attempts 1、backoff 5 秒；Embedding 为 openai_compatible / `auto` / `http://127.0.0.1:5002/v1`，timeout 120 秒、transport/index_operation retry 均为 3、backoff 5 秒。`resolved_model` 均为空，聊天 `context_window` 为空；不能补写未确认的 Embedding 模型名或上下文窗口。非凭据字段见 `models.json`，其中 `credential_env` 为教学变量名示例。
- Brain：UI 保存 hash `8ea4885f4eea`；启用感知、导航、世界动作、记忆检索和追加五个 MCP，本地 Skill 已按 UI 全文同步。
- Brain 归档比对：本地 `SKILL.md` 与 ZIP 的 `experiment/skills/items/s0001/SKILL.md` 在统一 CRLF/LF 和末尾换行后正文一致；不声称两份文件原始字节或 SHA256 相同。
- 已确认：预检 0 阻断、0 警告；回放初始画面完整显示自制背景、桌面散放状态、陈明远人物图，3 格显示设置生效。
- 已确认的前六步：Step 1 醒来起身，Step 2 穿衣，Step 3 整理床铺；Step 4 从 `[9,5]` 移到 `[13,7]`，实际提交 6 格路径；Step 5 到达书房站位 `[23,7]`；Step 6 提交“查看昨天研究笔记”的 ACT。Step 6 便签已累计确认起床三件事及抵达书房，工作书桌仍为 `scattered`。
- 已确认的整理过程：Step 7 区分已完成和待跟进资料；Step 8 按主题归类；Step 9 以独立 `SET_OBJECT_STATE` 把工作书桌改为 `tidy`，人物仍在 `[23,7]`。UI 截图明确显示散纸变为整齐本册。
- Step 10 的实际 ACT：在整理好的资料旁写下今天的优先事项，明确第一项研究方向。重启后回放仍保留 Step 9 的 `tidy` 和 Step 10 的动作事实。
- 已确认的厨房阶段：Step 11 选好今日材料；Step 12 从书房 `[23,7]` 移到家中通道 `[13,7]`；Step 13 向料理台 `[10,15]` 移动，实际先到厨房 `[12,16]`，剩余 3 格；Step 14 抵达 `[10,15]`。Step 15 取食材，Step 16 清洗切配，Step 17 开火烹煮，Step 18 检查熟度并调整火力，Step 19 装盘、准备前往餐区。这些均为独立真实动作，装盘尚不等于已移动到餐区。
- 已确认的餐区阶段：Step 20 到家中通道 `[13,16]`；Step 21 到餐桌旁 `[22,17]`；Step 22 摆好早餐；Step 23 用餐和喝水；Step 24 收拢盘杯餐具。Step 24 描述中出现“将餐具放回厨房方向”，但真实位置仍为 `[22,17]`，不能记成已经送回厨房。
- 已确认的返程与工作：Step 25 从餐区 `[22,17]` 向书房 `[23,7]` 提交 MOVE，按预算先到家中通道；Step 26 抵达 `[23,7]`，返程截图中的桌面仍为 `tidy`。Step 27 打开整理好的研究资料，Step 28 回看今日优先事项，Step 29 在 `08:24+08:00` 正式投入今天第一项研究工作；Step 30 继续阅读、核对证据并记要点，Step 31 梳理写作初稿，Step 32 补充关键论据并理顺段落，均在书房 `[23,7]`，未重启晨间流程。
- 状态事实：工作书桌 `object_key=game_object-mtsxqjy8-g4h5wd`；真实 payload 的 before 为 `state=scattered`、`materials_status=昨日资料散放`，after 为 `state=tidy`、`materials_status=资料已按主题归类，留出今日工作空间`，`actor_coord=[23,7]`。
- 已核对合计 5 项质量记录：Step 1 首次没有便签，出现 1 项 `EMPTY_MEMORY_RETRIEVAL`；Step 4 导航 `[23,7]` 超出视野 10 格，随后 MOVE `[13,5]` 不可走，共 2 项工具拒绝；Step 12 导航 `[10,15]` 和 Step 20 导航 `[22,17]` 各因超出视野出现 1 项工具拒绝。4 次能力拒绝均在本轮修正后提交正确动作，不能描述为无工具失败或零告警。
- 已修复的报告缺陷：原完成页面只汇总 Attempt 2 的两项，漏掉 Attempt 1 的三项，原因是每个 Attempt 重建 BrainRuntime 后仅用本 Attempt 诊断覆盖报告。修复从本 Run 已提交 frames 的纯文件事实重建质量投影，供最终化、回放读取和现有 UI 导出共用；57 项测试通过并已重启，原始世界事实未改。最终 UI 已完整显示 5 项，新导出 ZIP 与独立报告中的 5 项一致；无需重跑 32 步。
- 待改进的 SOP：远端最终站位超出视野时，应使用已经验证可走且位于当前视野内的途经点，不能猜门洞坐标。当前精确 Skill 保持与封存 Run 同版本，此处只记录改进点。
- 回放检查器修复已通过“缓存增长后选择角色”的浏览器复验。续跑过程中曾发现顶部显示旧 `PAUSED`、实际 Run 为 `RUNNING` 的状态来源混用；该问题后续已按 P01 修复，当前顶部只表示实验生命周期，Run 状态独立显示，见下方修复回归记录。
- 检查点日志：Step 13 出现一次旧检查点清理 `PermissionError`，随后自动恢复清理，`LATEST` 完整；这是清理瞬时错误，不能记成检查点损坏。
- 历史时间展示：恢复后的 Step 11 Action 摘要曾显示 UTC `23:27–23:30`，而页面标题为 `07:30`；Brain、IterationContext 和事实中的虚拟时间均为 `07:30+08:00`，3 分钟步长正确。该摘要展示与序列化问题现已按 P03 修复并在新的暂停恢复流程复验；历史日志不重写，不能把原显示问题记成仿真时钟错误。
- 成本记录：最终 UI 统计 108 次逻辑调用、108 次物理尝试、0 次物理重试，最长单次调用 66,965 ms；这与已发生的 4 次可恢复 MCP 能力拒绝是不同统计。完成页面总耗时 37 分 52 秒，包含约 4 分钟暂停修复等待，不是纯执行或推理基准。创建页面给出的 32–96 次调用、2–16 分钟估计对本次运行偏低，估时问题已记录、未处理。
- 尚存事项：远端目标途经点 SOP、日志级别、创建页面估时等 P04–P09。顶部状态来源混用、历史导出标注及 Action 摘要时区已在后续 P01–P03 回归中修复；原始历史日志保持原样。精确 Skill 保持 hash `8ea4885f4eea` 对应内容，未静默修订；出版图片与正式包协议复现仍需专项验收。
- 归档：浏览器新生成的结果 ZIP 与独立质量报告已原样复制到 [exports](../verification/exports/README.md)，复制后的 SHA256 与源文件一致。ZIP 共 114 个文件，连续 32 帧与 projection 哈希吻合，两个 Attempt 分别为 10/22 步，嵌入实验的 15 个文件及根哈希通过；ZIP 内质量报告与独立文件同字节、同为 5 项。
- 导出边界：普通结果 ZIP 完整嵌入实验和新质量报告，但不是正式 `.gaexp` 或 `.garun`，仅供审计与查看嵌入素材，不是一键恢复包。导出材料的文本、gzip 与 PNG 扫描未检出真实凭证、非空敏感字段或宿主绝对路径；保留 `credential_env` 引用，本地模型服务需自行配置。系统复现仍按浏览器步骤进行，旧报告与中途 ZIP 不复制到当前归档索引。

## 资料核对后的待决策事项

统一见 [问题与处理状态 P01–P09](../verification/pending-decisions.md)。资料核对轮次仅修正文档和本地教学参数；用户随后授权的 P01–P03 已由三个子 Agent 修复并完成合并检查与独立 6 步浏览器回归，见 [回归记录](../verification/p01-p03-regression.md)。精确 Brain 不变。其中历史制品元数据污染已修；WARNING 日志因 `error=PermissionError` 被 UI 误判为 ERROR 属于 P04，仍未修改。

运行估算的只读补核：两个 Attempt 排除人工暂停后的合计时长为 2,031.788 秒，约 33 分 52 秒，仍高于创建页上界；它包含两次 Attempt 的执行开销，并非纯模型推理时间。所有改进的证据、建议处理路径与验收条件以待决策清单为准。
