案例 key：book-case01-morning-routine
World：陈明远的家·背景版
Sector：住宅
地图网格：32×24，Tile 32×32。World 当前使用 4:3 住宅背景图 `assets/home-background.png`，浏览器导入记录显示源图为 1448×1086；实际显示比例和裁切仍需以画布验收为准。

空间布局（视觉方位）：
- 卧室：左上，`x=1,y=1,width=12,height=10`；L3 Arena「卧室」
- 书房：右上，`x=19,y=1,width=12,height=10`；L3 Arena「书房」
- 通道：中间纵向连接区，`x=13,y=1,width=6,height=21`；L3 Arena「家中通道」
- 厨房：左下，`x=1,y=12,width=12,height=8`；L3 Arena「厨房」
- 餐区：右下，`x=19,y=12,width=12,height=8`；L3 Arena「餐区」

浏览器已验证四段路线：`[9,5]→[23,7]` 16 格、`[23,7]→[10,15]` 23 格、`[10,15]→[22,17]` 14 格、`[22,17]→[23,7]` 21 格。它们是地图编辑器的路径检查记录，不替代实验运行中的实际移动结果。

当前实验出生点已在浏览器保存为 `[9,5]`，地址同步为卧室 > 床边。已知空间还保存了书房工作书桌、厨房料理台、餐区早餐餐桌和家中通道空物件；当前 32 步 Run 已确认卧室→书房→厨房→餐区→书房的真实移动，分步事实见 `../settings/verification-record.md`。这不替代出版近景和所有站位遮挡的专项验收。

四层地址以稳定空间节点身份及包内语义索引为依据，运行时不通过逐 Tile 重复拼接语义树；通道是独立的纵向连接区。主目录中的地图 JSON 仍是历史参考，不能用于当前录入；以 `README.md` 中文配置清单为准，最终浏览器结果 ZIP 内的嵌入实验仅作为已验证事实的审计来源，见 `../verification/exports/README.md`。
碰撞只由地图导航显式记录：外墙、房间隔断和非门洞墙格阻挡移动；床、书桌、炉灶和早餐桌的视觉占位采用整数 Tile 配方，站位不依赖图片边界。

已确认的 Game Object 边界：床边 `8,3,3,5`；工作书桌 `24,5,6,3`；料理台 `4,12,8,6`；早餐餐桌 `22,14,6,5`。工作书桌的整理前状态为默认状态，顶层 `state=tidy` 指向整洁桌面切片。
