# 案例 1 地图素材

## 文件

| 文件 | 尺寸 | 用途 | 当前状态 |
| --- | --- | --- | --- |
| `home-background.png` | 1448×1086 | World 的完整住宅背景：卧室左上、书房右上、厨房左下、餐区右下，中间为纵向通道 | 已复制到案例目录，通过浏览器绑定 World，回放完整显示；结果 ZIP 已归档 |
| `desk-scattered-source.png` | 1448×1086 | 以住宅背景为参考、仅改变书桌区域的整理前源图 | 已导入系统，并创建了 `x=34,y=7,w=8,h=4` 的切片 |
| `desk-tidy-source.png` | 1448×1086 | 以同一住宅背景为参考、仅改变书桌区域的整洁状态源图 | 已导入系统，并创建了同样的 `x=34,y=7,w=8,h=4` 切片 |

## 来源与提示词摘要

三张图由内置 `imagegen` 生成或编辑后保存为本案例素材。底图提示词要求：4:3 的完整住宅俯视像素场景、统一家具和角色尺度、卧室/书房/厨房/餐区围绕中央纵向通道布置，不包含文字、网格、人物或界面元素。两张桌面状态图以同一底图为参考，只编辑书房书桌区域，保持镜头、房间、家具位置和画布尺寸不变，分别表现资料散乱和桌面整洁。

## 验收状态

- 工作书桌 Game Object 当前为 `x=24,y=5,w=6,h=3`，默认状态使用整理前切片，顶层 `state=tidy` 指向整洁切片；编辑器中已实际切换并恢复为整理前。
- 当前 Run 第 9 步已通过独立 `SET_OBJECT_STATE` 把 `scattered` 改为 `tidy`，真实 payload 与截图均已核对，画面从散纸切为整齐本册，人物仍在 `[23,7]`。该变化来自合法对象状态事实，不是 ACT 文本推断。
- 初始回放确认底图在 32×24 地图中完整显示，最终结果 ZIP 中的嵌入素材已校验归档；出版所需的四处人物近景仍待补齐。
- Agent 头像和行走图已在浏览器中上传、保存、应用到本案例人群并复制进当前实验；3 格显示设置与卧室→书房→厨房→餐区→书房的实际移动已确认，各处近景的比例细节、脚底锚点与遮挡仍待出版专项验收。
- 当前 Run 已在回放角色详情缓存修复后从原检查点续跑并完成 32 步，晨间生活闭环已走通；跨 Attempt 质量报告与最终浏览器导出均已复验。当前 Run、告警和历史问题见 `../../settings/verification-record.md`，导出索引见 `../../verification/exports/README.md`。
