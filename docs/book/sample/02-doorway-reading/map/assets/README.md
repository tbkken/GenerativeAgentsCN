# 阅读空间背景素材

本例正式背景是 `reading-room-background.png`，由主 Agent 使用内置 `image_gen` 为本案例全新生成，已保存到本目录。它是一张完整的双场所室内背景，不来源于其他实验地图，也不需要在运行时引用工具临时目录。

| 项目 | 实测值 |
| --- | --- |
| 文件名 | reading-room-background.png |
| 尺寸 | 1536×1024 像素，宽高比 3:2 |
| 文件大小 | 2,348,708 字节 |
| 像素格式 | Format24bppRgb，无 alpha 透明通道 |
| SHA256 | `1a81264c8f2033b041a19f763944dffe568ac7398f9c6ec157aa87c8c52585fc` |
| UI 已保存使用方式 | 原图以 48×32 素材源网格创建完整切片“门廊与阅读室完整背景”，绑定 World 最底层，24×16 逻辑网格、32px 逻辑 tile |

已通过图片查看工具目视实际 PNG：画面使用暖色、清楚的像素风；左侧是浅色石砖地板的准备区域，有长凳、边柜和挂钩；右侧是木地板阅读角，有书柜、单人桌椅、台灯和植物。中间竖直分隔墙在中央留出一处门洞。背景没有人物，适合叠加本例林晨行走图。

源图分隔墙约位于 x620–670，上半墙止于 y450 附近，下半墙从 y596 附近开始。此范围是目视估计，用于提出网格候选，不是图片自动生成的碰撞合同。源图每 64×64 像素对应候选地图的一格；World 全图缩放为 768×512 逻辑像素，不改变地图宽高比。

书柜、桌椅和墙体已经画在背景里。主 Agent 已通过浏览器完成图片上传、完整切片与 World 绑定，另行保存六节点与碰撞，地图校验及两点路径检查通过；只有 World 使用这张背景，其他节点没有额外素材或被动 Skill。图片内容本身不自动生成对象与通路，当前空间配置是 UI 明确录入的结果。正式正常流程已核对本轮人物穿门、桌旁比例及实际 4+4+2 格 MOVE，见 [正式运行记录](../../verification/formal-run-record.md)；背景已嵌入导出实验并通过完整性核验，见 [导出记录](../../verification/exports/README.md)。全部遮挡与出版近景仍单独验收。
