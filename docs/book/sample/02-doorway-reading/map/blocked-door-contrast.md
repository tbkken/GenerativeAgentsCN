# 门洞封闭对照的地图差异

对照仅改变碰撞配置，不更换背景或空间语义。背景仍绘有开放门洞，所以它是用于教学的“图片不决定通行”条件对照，不将画面描述为已经换成关门图片。

| Tile（从0开始） | 正式地图 | 封门副本 | 公共地图恢复后 |
| --- | --- | --- | --- |
| (9,8) | 可走 | 阻挡 | 可走 |
| (10,8) | 可走 | 阻挡 | 可走 |

2026-09-09，经浏览器打开公共地图 `fe34ac77-2178-48fa-ae90-2dab98786d4e` 的“通路”工具，选择“阻挡”，逐格点击上述两格并保存；没有编辑其他格。两点路径检查起点(7,7)、终点(16,7)，实际返回“不可达：两点不连通”。

随后在浏览器创建实验 `37378809-78d2-45c2-a4b6-5653f0f44142`（案例2对照：封闭门洞后的阅读桌可达性），将此时的地图物理导入其独立副本。看到“独立实验草稿已创建”后，立即回到公共地图选“可走”，恢复同两格并保存。21:01页面显示已与服务器同步；同样的两点路径重新检查为“可达 · 11格”。

正式8步实验与此前边界实验已拥有自己的地图副本。本次公共资源的临时变化不会被当成修改这些实验的手段；封门对照运行及下载副本仍需核验实际碰撞和导航结果，结果归档到 `verification/contrasts/blocked-door/`。
