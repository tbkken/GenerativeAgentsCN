# 案例 2 地图录入与验收

**状态：地图已导入，正式正常流程的 8 帧移动、碰撞和门洞/桌旁画面已核验；边界对照仍在进行。** 正式地图为“林晨的门廊与阅读室”，ID `fe34ac77-2178-48fa-ae90-2dab98786d4e`，key `book-case02-doorway-reading`。背景是 `assets/reading-room-background.png`，1536×1024 PNG、2,348,708 字节，来源与 SHA256 见 [素材说明](assets/README.md)。B02-006 修复后初始 (7,7)/门廊保存成功，正式对象导航经唯一门洞到 (16,8)，证据见 [正式运行记录](../verification/formal-run-record.md)和 [导出核验](../verification/exports/README.md)。

场景是相邻的门廊与阅读角，中间是一堵清晰可辨的墙和一处门洞。使用一张完整室内背景叠加 `World → Sector → Arena → Game Object` 四层空间语义。本例只建立置物边柜、阅读桌两个对象；其他家具通过背景与碰撞表达。只有 World 绑定完整背景，其余节点无额外素材、无被动 Skill。本例不演示对象状态换图。

设计时把整条短路线与人物一起校准：整理书包的起点、门前可走位置、门洞、门后位置、阅读桌旁站位都需要与背景、碰撞和语义边界对应。阅读桌图片的中心不应直接当可走终点，最后的交互站位以系统导航结果和浏览器验证为准。

## 已保存网格与本轮画面验收

当前 UI 已保存 **24×16 逻辑网格、32px 逻辑 tile**。完整原图以素材源网格 **48×32** 创建完整切片“门廊与阅读室完整背景”，绑定到 World 并缩放铺满；主 Agent 的截图已显示整张图，保持原始 3:2 宽高比。因此每一逻辑格对应源图 64×64 像素，World 的逻辑画布为 768×512；没有预先裁剪或改写原图。

林晨行走图单帧是 32×32，透明边缘较大，人物轮廓小于整帧。主 Agent 已保存 **显示 2.5 格**，对应整帧 80×80 逻辑像素；人物实际轮廓更小。正式逐步回放已核对本轮穿门与阅读桌旁的比例，人物没有填满门洞，脚底位于地面。本例没有独立坐姿/阅读动画，ACT 使用现有角色姿态与活动文字；出版近景和全部遮挡仍是独立验收项。

门口应让完整角色轮廓看起来可以通过，角色脚底应落在可走地板上；视觉门宽与碰撞门宽分别验收。实际图片中门洞位于源图约 x620–670、y450–596。正常地图将 x9–10 的 y0–7 与 y9–15 设为阻挡，**仅保留 y8 为门洞**，替代初始 y7–8 两行候选。正式 MOVE 已通过 (9,8)、(10,8)，回放与导出路径证据一致。

完整碰撞、验证边界与历史候选演变见 [空间与碰撞记录](spatial-collision-candidates.md)。浏览器两点路径检查 **(7,7) → (16,7)** 返回 **可达、11 格**，终点可走，并绕行唯一 y8 门洞。这个静态查询尚不是角色 Run 中的导航或已提交 MOVE。

## 已保存的六个空间节点

下列坐标使用 UI 的 x、y、宽、高。精确语义正文与真实感知确认的稳定 ID 见 [节点录入原文](semantic-nodes.md)；名称和范围已保存，不再使用早期候选名称。

| 层级 | 节点名称 | 父节点 | x | y | 宽 | 高 | 素材与被动 Skill |
| --- | --- | --- | --- | --- | --- | --- | --- |
| L1 World | 林晨的门廊与阅读室 | 无 | 0 | 0 | 24 | 16 | 完整背景；无被动 Skill |
| L2 Sector | 馆内阅读空间 | 林晨的门廊与阅读室 | 1 | 1 | 22 | 14 | 无 |
| L3 Arena | 门廊 | 馆内阅读空间 | 1 | 1 | 10 | 14 | 无；范围包含门洞 |
| L3 Arena | 阅读角 | 馆内阅读空间 | 11 | 1 | 12 | 14 | 无 |
| L4 Game Object | 置物边柜 | 门廊 | 3 | 2 | 3 | 3 | 无 |
| L4 Game Object | 阅读桌 | 阅读角 | 16 | 5 | 6 | 4 | 无；包含可走桌旁列 x16 |

完整阅读桌地址为 `林晨的门廊与阅读室 → 馆内阅读空间 → 阅读角 → 阅读桌`。阅读桌 GO 包含 x16–21，但桌椅仅阻挡 x17–21；**不能把整个 GO 都涂成碰撞**。置物边柜 GO 的 y4 同样不在柜体阻挡 y2–3 内。语义范围、家具实体和角色站位须分别理解。

## 验证状态与待补资料

| 项目 | 当前记录 |
| --- | --- |
| 地图名称、稳定 ID、key | 已保存；见上文与 `settings/browser-building-record.md` |
| 背景文件名、源图尺寸、SHA256 | 已记录于 `assets/README.md`；主 Agent 已通过浏览器上传并绑定 World |
| 逻辑网格、tile 尺寸、背景切片 | 已保存 24×16、32px；48×32 素材源网格完整切片绑定 World |
| 六节点名称和边界 | 已保存；见上表 |
| Game Object 稳定 ID、精确语义正文 | 见 semantic-nodes.md；Step 1 实际感知返回阅读桌 `game_object-mttvfdsu-3ijqb3`、置物边柜 `game_object-mttvfs3a-ft6abi` |
| 起点、门洞与阅读桌旁站位 | UI 已检查 (7,7) → (16,7)，唯一门洞 x9–10/y8，11 格可达 |
| 碰撞与地图校验 | 已浏览器绘制保存，地图校验通过，有主 Agent 截图及 AX 观察 |
| 角色感知、MCP 导航、实际 MOVE 路径 | Step 1 感知返回阅读桌完整四层地址；正式对象导航到 (16,8)，实际 10 格、三轮 4+4+2，路径连续且避开碰撞；到达后才取书与阅读 |

正常教程先保证门洞连通。门被阻挡、只给简称、请求超出视野或注意力带宽等属于独立对照；按合同产生的拒绝不是系统 bug。发现超出预期的系统问题须按仓库规则阻断并报告，不靠换地图或扩大视野绕过。
