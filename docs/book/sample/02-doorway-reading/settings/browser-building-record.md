# 案例 2 界面构建记录

**状态：B02-006 已闭环，正式正常流程已完成并通过 8/8 步验收；边界对照仍在进行。** 下列构建事实来自主 Agent 的浏览器操作反馈、截图与 AX 观察。本地资料准备不代替这些操作；后续运行与导出分别见 [正式运行记录](../verification/formal-run-record.md)、[导出核验](../verification/exports/README.md)，不将正常流程通过等同于整个案例 PASS。

## 已完成：创建地图并绑定完整背景

| 项目 | UI 实际保存值 |
| --- | --- |
| 地图名称与 World 名称 | 林晨的门廊与阅读室 |
| 地图稳定 ID | `fe34ac77-2178-48fa-ae90-2dab98786d4e` |
| 地图稳定 key | `book-case02-doorway-reading` |
| 逻辑网格 | 24×16 |
| 逻辑 tile | 32px |
| 上传图片 | 本例 `map/assets/reading-room-background.png`，完整 1536×1024 |
| 素材切片名称 | 门廊与阅读室完整背景 |
| 素材源网格与切片 | 48×32 格，使用完整图像切片 |
| 绑定位置 | World，覆盖其 24×16 逻辑范围 |
| 保存后视觉 | 主 Agent 的浏览器截图已显示整张双场所背景 |

源素材的 48×32 格与地图逻辑 24×16 格是两种用途：前者定义完整素材切片，后者定义世界空间。原图全图映射为 World 768×512 逻辑像素，因此每个逻辑 tile 对应源图 64×64 像素；不能误把 48×32 写成地图逻辑尺寸。

## 已完成：六节点与碰撞

World“林晨的门廊与阅读室”、Sector“馆内阅读空间”、Arena“门廊”“阅读角”、Game Object“置物边柜”“阅读桌”共六个节点已保存。精确 x/y/宽/高见 `map/README.md` 六节点表，只有 World 使用完整背景，其他节点无额外素材或被动 Skill。

阅读桌 GO 为 x16–21/y5–8，桌椅碰撞为 x17–21/y5–8，**GO 内保留可走列 x16**。置物边柜 GO 为 x3–5/y2–4，柜体仅阻挡 y2–3。中墙 x9–10 阻挡 y0–7 和 y9–15，唯一门洞为 y8，归属门廊 Arena。外圈和家具碰撞已浏览器绘制保存，完整清单见 `map/spatial-collision-candidates.md` 当前记录。

## 已完成：静态页面验证

- 主 Agent 完成地图校验，页面通过，有截图与 AX 观察。
- 两点路径检查 **(7,7) → (16,7)** 返回 **可达、11 格**。
- 页面返回终点可走及素材检查结果，路径绕行唯一 y8 门洞。

此查询不是正式 Agent 的 `world-navigate` 或实际 MOVE。静态查询的逐格路径未归档，不根据 11 格长度补造。后续正式对象地址导航选择不同终点 (16,8)，其实际 10 格路径、预算消费和到达事实已在正式运行记录与原始导出中核验。

## 已完成：独立公共 Agent 与单人人群

主 Agent 已在 UI 新建并保存林晨，key `agent-mttvoove-oxit70`，21 岁；从本例 `agents/林晨/assets/portrait.png` 与 `texture.png` 上传头像和行走图，保存显示 2.5 格、vision_radius 10、attention_bandwidth 6。

已保存 Crowd“案例2：林晨穿过门口去阅读”，ID `c8751983-07e5-4a41-bf2c-1c7b52eff796`、key `crowd-2-b8aa4c8580d6`，成员只有上述林晨。这个角色是本例独立创建的公共资源，不能按同名人物误选其他案例 Agent。

当前目标保存原文：

> 已经带着书包站在门廊空地，先确认书本、笔记本和笔，随后观察并走到阅读桌旁。真实抵达以后再取书、浏览章节、安静阅读和记笔记，不把查好路线当成已经抵达。

## 已完成：正式 Brain

UI 已保存 Brain ID `bcfada37-6dd6-4152-b172-e4e3d96d1e1d`，key `book-case02-doorway-reading-brain`，页面内容哈希 `743a968cf2b2`。主 Agent 已把实际填入 UI 的同文保存到 `skill/SKILL.md`。

MCP 页面确认 5 项：world-perceive、world-navigate、world-act、memory-stream-search、memory-stream-append；0 脚本、0 子 Skill。正式 Brain 使用真实感知的完整 `target_address` 导航及 MOVE，不填写 `target_coord`，不假设导航返回完整路径或最终坐标。旧 `reading-sop-draft.md` 仅作为未执行历史草案保留并标注纠正。

## 已完成：创建正式实验并保存参数

实验 ID `c63b2d81-433a-4c2f-9d75-b44bcf04e318`，名称“案例2：穿过门口，到阅读角看书”。UI 已正确导入本例地图、人群与 Brain，1 名 Agent。

| 参数 | UI 已保存值 |
| --- | --- |
| 虚拟起始时间 | 2026-09-09 09:00，Asia/Shanghai |
| 总步数 / 步长 | 8 步 / 1 分钟 |
| 随机种子 | 42 |
| 检查点频率 / 保留 | 1 步 / 2 份 |
| Agent Step 投影间隔 | 1 步 |
| 模型 Payload | 开启 |
| 角色图片 | 头像与行走图均在实验 Agent 中显示“已复制进当前实验” |
| 实验角色显示 / 感知 | display=2.5，vision=10，attention=6 |

## 历史阻断：B02-006 初始位置同步保存失败

实验创建时默认保存的初始位置是 **(3,4)**，地址为“林晨的门廊与阅读室 → 馆内阅读空间 → 门廊 → 置物边柜”。以下保留首次修改失败的历史，当前位置以修复后复验结果为准。

主 Agent 在实验 Agent 编辑弹窗把坐标改为 **(7,7)**，UI 坐标语义显示“林晨的门廊与阅读室 → 馆内阅读空间 → 门廊”，只有三层。点击 **“同步为初始位置”** 后再保存，toast 返回：

> Agent agent-mttvoove-oxit70 needs one four-level initial_location

当时 **(7,7) 保存失败，持久化初始位置仍是 (3,4)**。主 Agent 停在该编辑弹窗，等待用户批准后才交工程修复，没有启动 Run 或改用其他位置绕过。完整问题证据见 `verification/blockers.md`。

## 已完成：B02-006 修复后原路径复验与正式启动

- 工程修复后 29 项针对性测试通过。主 Agent 执行 `restart-web.bat`，Web PID **40368 → 42456**，健康检查通过。
- 同一实验、同一 Agent 的原编辑弹窗点击“同步为初始位置”后保存，UI 提示 **“Agent 已保存”**，列表显示 **7,7**。
- 重新打开编辑器，初始坐标仍为 **(7,7)**，初始地址为 **林晨的门廊与阅读室 → 馆内阅读空间 → 门廊**；已知空间仅同一门廊，物件列表为空，没有虚构第四层对象。
- 预检显示 **0 阻断、0 警告、0 自动检查、1 通过**。随后主 Agent 确认正式执行 8 步，实验封存并创建 Run。
- 正式 Run：`b4b4ca09-87bd-4980-bdc5-8146474d6225`；唯一 Attempt：`881415da-972b-4831-8df5-a2ede8d62bcb`。启动时曾记录运行中 1/8；后续已核对终态 **COMPLETED，8/8**。
- 逐步回放已核对背景、人物、唯一门洞及阅读桌旁的实际位置；2.5 格人物的脚底位于地面，未填满门洞。本例未配置独立坐姿或阅读动画，普通 ACT 仍用现有角色姿态展示。

## 已完成：正常流程、质量与导出

Step 1 在 (7,7) 整理书包，Step 2–4 三次 MOVE 消费 4+4+2 格，到达 (16,8) 后 Step 5–8 才取书、浏览章节、阅读和摘记。共 33 次逻辑调用、33 次物理尝试，0 次重试；36 次 MCP 调用全部无拒绝。唯一质量项为 Step 1 首次空记忆检索，后续已读回进度便签。UI 耗时 5 分 48 秒，未发生暂停恢复。

主 Agent 通过 UI 导出已完成 Step 8 的原始结果 ZIP 和独立质量 JSON，文件已归档，哈希、质量来源、嵌入实验 13 文件完整性、8 帧事件与路径一致性均已核验，详见 [导出核验清单](../verification/exports/README.md)。包内 Brain 与本地正文文本一致，LF/CRLF 差异导致字节哈希不同；未改写原始文件。

## 尚未完成

边界对照仍在进行，相关结果单独登记，未全部完成前不标记整个案例 PASS。正式 `.gaexp` / `.garun` 独立交付、异机导入恢复与出版近景验收也未完成；普通结果 ZIP 的核验不能替代这些项目。

精确语义正文与稳定节点 ID 已归档于 [语义节点记录](../map/semantic-nodes.md)，正式 Skill、人设和参数已按 UI 与导出证据同步。正常流程已有真实感知、MCP 导航、按预算 MOVE、到达后 ACT、终态和质量证据，不再列为“尚未运行”。

## 保留的设计演变

“只有 World”是上一构建阶段，现已推进为六节点地图。初期两行门洞 y7–8 已改为仅 y8，阅读桌 GO 向左扩入 x16；原候选阅读点 (16,6) 改为本次实际检查的 (16,7)，纸面约 10 格改用 UI 返回的 11 格。旧候选在 `map/spatial-collision-candidates.md` 历史区保留，不再作为当前录入依据。
