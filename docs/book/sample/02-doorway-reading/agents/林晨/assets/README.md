# 林晨图片来源与校验

这两张 PNG 于 2026-09-09 从仓库已有用户角色素材目录 `docs/images/creek-university-v2/agents/林晨/` 原样物理复制到本目录。来源仅用于追溯；本例上传使用本目录文件，不依赖来源目录在运行时存在。

没有采用该来源 `agents/` 根部较早的 `lin-chen-walk-4x4.png` 或大头像，也没有复制校园人物旧 JSON、日程和坐标。

| 文件 | 实测尺寸 | 大小 | SHA256 |
| --- | --- | --- | --- |
| portrait.png | 32×32 | 987 字节 | `2dfa074ecd94cb5a9d8a60d48fb80277f97f664037601919f944d0b27b470b2c` |
| texture.png | 96×128 | 8,251 字节 | `de7d8f0abdf94d221248f5a24e4c777a038059465713373146ca02ea0df587f7` |

复制前后 SHA256 一致。两图均实测为 `Format32bppArgb`；左上角像素 alpha=0，头像中心与行走图正面静止帧中心像素 alpha=255，证明具有实际透明背景和不透明人物像素，不是绘制的棋盘格背景。

已用图片查看工具分别目视两张实际文件：深色短发、青绿色上衣、深色下装的像素学生。行走图是 3 列×4 行、每帧 32×32，共 12 帧；方向为下、左、右、上，列为左脚步、静止、右脚步。正式 8 步回放已核对本轮路线中人物穿门、桌旁位置、2.5 格比例和脚底贴地，见 [正式运行记录](../../../verification/formal-run-record.md)。这不等于所有方向与出版近景均已验收，也没有独立坐姿/阅读动画。两张图片均已嵌入导出的实验并通过完整性核验，见 [导出记录](../../../verification/exports/README.md)。
