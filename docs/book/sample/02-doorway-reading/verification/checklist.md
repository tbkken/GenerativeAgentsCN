# 案例 2 验收清单

**最新停点：B02-007，封门对照回放资源超时，等待用户评估；未标全案PASS。** 正式运行与边界对照下载均已归档，封门运行已完成、MCP不可达已确认，画面与导出副本尚未验收。

**状态：B02-006 已闭环，正式正常流程已 `COMPLETED 8/8`；边界对照仍在进行，整个案例尚不标记 PASS。** 勾选仅表示有对应实际证据，正常流程以 [正式运行记录](formal-run-record.md)和 [原始导出核验](exports/README.md)为准，不用静态地图检查或诊断 Run 代替正式结果。

## 本地资料准备

- [x] 本例独立 `map/`、`agents/`、`skill/`、`settings/` 与 `verification/` 目录已建立。
- [x] 林晨头像与行走图已物理复制到本例；尺寸、RGBA 透明像素与 SHA256 已核查。
- [x] 已目视林晨实际 PNG，确认 32px 像素角色与 3 列×4 行布局。
- [x] 本例新背景已生成并保存到 `map/assets/reading-room-background.png`，实测 1536×1024、2,348,708 字节，SHA256 已记录；仅完成素材核查。
- [x] 正式 Brain 已 UI 保存，精确正文归档到 `skill/SKILL.md`；内容显示哈希 `743a968cf2b2`，5 项 MCP，0 脚本、0 子 Skill。
- [x] 正常流程的人设、实际模型参数、地图语义、节点稳定 ID、运行结果与导出核验已同步；本地 Brain 与包内文本一致，LF/CRLF 字节差异已明确。

## 页面与视觉验收

- [x] 主 Agent 在浏览器创建地图 `fe34ac77-2178-48fa-ae90-2dab98786d4e`，上传完整背景，以 48×32 源网格完整切片绑定 24×16 World 并保存；截图显示完整背景。
- [x] 主 Agent 从本例目录 UI 上传 portrait.png 与 texture.png，保存独立林晨 `agent-mttvoove-oxit70`，21 岁、显示 2.5 格、视野 10、注意力 6。
- [x] 已保存单人人群“案例2：林晨穿过门口去阅读”，ID `c8751983-07e5-4a41-bf2c-1c7b52eff796`，唯一成员为本例林晨。
- [x] 已创建实验 `c63b2d81-433a-4c2f-9d75-b44bcf04e318`，正确导入本例地图、人群、Brain，1 Agent；角色两图 UI 显示“已复制进当前实验”，display2.5/vision10/attention6 已确认。
- [x] 原始结果 ZIP 中的嵌入实验已核对三张图片、Brain、参数、13 文件完整性与协议；此项不代表正式 `.garun` 或异机恢复验收。
- [x] 本轮起点、门口与桌边的回放已核对人物比例、脚底贴地及通行画面；未配置独立坐姿/阅读动画，不扩大为全部姿态与出版画面通过。
- [x] 六个空间节点和边界已 UI 保存，只有 World 有背景；门廊包含门洞，阅读桌 GO 包含可走列 x16。精确语义与已核对稳定 ID 见 [节点记录](../map/semantic-nodes.md)。
- [x] 碰撞已 UI 保存，地图校验通过；唯一门洞 x9–10/y8，两点检查 (7,7) → (16,7) 返回可达 11 格、终点可走及素材结果。
- [x] 正式 Step 1 感知返回阅读桌 `game_object-mttvfdsu-3ijqb3` 和完整四层地址，距离 9、实际半径 10、未截断。
- [x] 实际 MOVE 经过唯一门洞 (9,8)/(10,8)，到达桌旁 (16,8)；路径与回放位置一致。
- [x] B02-006 修复后，同一 Agent 的初始 (7,7) 与真实门廊三层地址保存、重开核对通过；旧 (3,4)/置物边柜仅保留历史。
- [x] 实验已知空间仅同一门廊，物件列表为空；没有虚构对象或预置阅读桌。正式 Step 1 通过实际感知获得阅读桌地址。
- [x] 实验参数已 UI 保存：2026-09-09 09:00 Asia/Shanghai，8 步×1 分钟，seed42，checkpoint1/retain2/projection1，模型 Payload 开启。
- [x] 原实验预检显示 0 阻断、0 警告、0 自动检查、1 通过，随后封存并确认执行。
- [x] 正式 Run 与唯一 Attempt 已完成，背景、角色、门洞及桌旁画面已核对。
- [x] 实验已 SEALED，Run 已 COMPLETED 8/8；质量仍有 1 条首次空记忆警告，三个维度分别记录。

## 8 步正常过程实测结果

以下按可观察的阶段验收，不把阶段编号当 Step 排程。实际需要多少 MOVE，由路径长度与预算决定。

| 观察阶段 | 必须核对的证据 | 结果 |
| --- | --- | --- |
| 在门廊整理书包 | ACT 的 Event、非空 payload、真实坐标和当前地址一致 | Step 1 ACT 整理书包，(7,7)/门廊，path=[]，accepted=true |
| 感知并确认目的地 | 来自真实可见对象的完整四层地址；不由简称猜出层级 | Step 1 实际返回阅读桌 key 和完整地址，距离 9；初始知识未预置该对象 |
| 只读查询路线 | 导航查询不提交世界移动；实际到达以 MOVE 的事实核对，不把 next_coord 当最终目标 | 共 4 次 navigate，0 次拒绝；Step 5 同对象导航实际返回 reachable=true、distance_tiles=0、next_coord=[16,8]、movement_required=false，随后才取书；世界位置变化来自 MOVE |
| 向门口并穿门移动 | 实际 MOVE 路径逐段可走、未穿墙；每步长度不超过真实预算 | Step 2–4 实际 4+4+2 格，经 x9–10/y8；逐格连续性和碰撞检查通过 |
| 移动预算未足以到达时 | 存在明确剩余路段，下一轮继续行程；没有提前执行阅读 | 前两次落点 (10,8)/(14,8)，余程 6/2；进入阅读角仍继续 MOVE |
| 到阅读桌旁取书 | 坐标确已抵达有效站位，当前地址属于阅读角，再提交 ACT | Step 4 到 (16,8)/阅读桌；Step 5 才 ACT 取出书本和笔记本 |
| 浏览、阅读与笔记 | 至少可辨认阅读和笔记活动；ACT 文本不伪造无输入的具体书页事实 | Step 6 浏览章节、Step 7 阅读一小节、Step 8 记录阅读摘记；无虚构具体书页内容 |
| 回放和运行事实 | MOVE 轨迹、当前事件、语义地址与 StepResult 相符，每步至多一次成功世界动作 | 连续 8 帧，每帧 1 个唯一世界事件，SPO 和非空 payload、地址与位置一致 |

UI 保存 8 步、每步 1 分钟，B02-006 修复且预检通过后才确认执行。静态 (7,7) → (16,7) 为 11 格，正式对象地址导航到 (16,8) 为 10 格；终点不同，长度分别记录。运行依真实进度推进，不按指定步号强迫到达。Brain 的 SOP 要求先等待工具结果再决定下一次调用，本轮未用 WAIT 凑步数。SOP 要求与实际模型调用次数不能混为一项证据。

## 对照实验（分别记录，不混入正常教程）

边界对照正在单独归档；下列待办不以正常 Run 的成功代替。残缺多元素地址与单元素简称输入分别记录，不能互相冒充覆盖。

- [ ] 门洞被阻挡：MCP实际返回不可达且ACT留在(7,7)，已读到；回放与原始导出核验因B02-007未完成。
- [x] 残缺地址：实际`["阅读角","",""]`被正确拒绝，已归档；原计划单元素`["阅读角"]`未精确覆盖，不能混称。
- [x] 请求超出 `vision_radius`：实际请求100格，生效10格、硬上限10格。
- [x] 候选超过 `attention_bandwidth`：带宽6→1后GO输出2→1、Event3→1，CURRENT的World/Sector/Arena三层锚点保留。

## 运行与交付

- [x] 正式身份已登记：Experiment `c63b2d81-433a-4c2f-9d75-b44bcf04e318`，Run `b4b4ca09-87bd-4980-bdc5-8146474d6225`，唯一 Attempt `881415da-972b-4831-8df5-a2ede8d62bcb`；COMPLETED 8/8，步长 1 分钟，2026-09-09 09:00 Asia/Shanghai 开始。
- [x] 唯一质量 WARNING 为 Step 1 `EMPTY_MEMORY_RETRIEVAL`；36 次 MCP 调用无拒绝，33 次物理尝试无重试，分别统计。
- [x] 33 次逻辑调用、33 次物理尝试；UI 5 分 48 秒，Attempt 持续 348.274705 秒，本轮未暂停恢复；最长单次模型延迟 26529 毫秒。
- [x] UI 导出原始 ZIP 和独立质量 JSON，大小、SHA256、Step 8/COMPLETED 来源与生成时间均已归档，见 [exports/README.md](exports/README.md)。
- [x] UI 质量、独立报告、ZIP 内质量及唯一 Attempt 一致；嵌入质量 JSON 与独立文件逐字节相同，8 帧事实核对通过。
- [x] 交付边界已明确：普通结果 ZIP 与嵌入实验完整性已核验；正式 `.gaexp` / `.garun` 独立交付、异机恢复及出版近景尚未验收。
- [x] 正常流程结论已更新根 README；草案和历史诊断不冒充当前录入依据。
- [ ] 边界对照及其资料全部验收完成后，再评估整个案例 PASS。
