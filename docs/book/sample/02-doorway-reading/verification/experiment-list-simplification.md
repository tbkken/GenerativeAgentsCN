# 实验中心列表精简验收

日期：2026-09-09。状态：前后端清理完成，重启后列表浏览器验收通过。

用户明确要求删除功能，而不是隐藏按钮或继续修补搜索/保存视图。本次修改由工程子 Agent 完成，主 Agent 使用浏览器配置和验收。

## 删除范围

- 顶部全部、草稿、已封存、最近 Run 各状态数量的整行，以及同一行搜索框。
- 搜索、负责人/标签/模型/地图等组合筛选与可选排序。
- 比较、添加标签、转移负责人。
- 页容量选择器，改为固定每页 5 条；保留分页。
- 紧凑表格、保存视图、已保存视图、删除视图。
- 上述功能专用的事件、状态、弹窗、样式、接口及无用测试；临时 B02-003 内存诊断。

## 保留范围

新建、打开、复制、删除、归档/恢复与必要批量选择；当前/已归档/全部的归档管理入口；默认最近更新顺序；每页 5 条及翻页。实验定义中的 owner/tags 元数据与 Run/回放不属于本次删除范围。

## 浏览器验收范围

1. 新加载实验中心：无被删控件和状态计数行，卡片上方没有残留空白布局。
2. 第一页最多 5 张卡片，末页数量正确；上一页/下一页边界正确。
3. 打开已有实验再返回列表，不产生模型名筛选或空列表。
4. 基本管理可用；用本任务创建的无 Run 回归草稿验证归档、已归档入口、恢复，不更动用户其他实验。
5. 重载后仍固定每页 5 条，临时诊断与保存视图逻辑不再出现。

当前已知系统资源：B02-002 无 Run 回归草稿 `41c2649d-a3a9-4a02-9f8f-168aa6e11239` 可用于可逆归档/恢复；B02-001 原诊断草稿 `837e7a6f-885f-489f-8074-2d20ec6acb1b` 仍待配置，不能直接运行默认 1000 步。

## 验证结果

- 工程子 Agent `fix_b02_003` 完成前端控件、事件、状态、专用弹窗和样式，以及后端专用 API、服务和 ORM 类清理。`/api/studio` 和旧 `/api/v1` 中比较、保存视图及标签/负责人批量操作入口均已删除。历史已保存视图数据未执行清库或迁移。
- 工程验证：45/45 前端测试、7 项针对性 Python 测试通过；JavaScript 语法、Python 编译和 `git diff --check` 通过。新增回归覆盖固定分页、过期请求隔离、删除末页、归档恢复、被删除的接口以及非空 owner/tags 保留。
- 主 Agent 执行 `restart-web.bat`，退出码 0，健康检查成功，Web PID 37344 → 53552。未启动、取消或恢复任何 Run。
- 重启后浏览器 `http://127.0.0.1:8000/` 的 AX 树与截图确认：指定控件及顶部状态计数/搜索整行消失，标题下直接是基本归档管理和实验卡片，没有旧行占位。当前验收视口约 585 × 465；没有宣称完成宽屏下全部卡片内容的视觉验收。
- 当前清单共 9 个实验：第一页显示 1–5，上一页禁用；第二页显示 6–9，下一页禁用。打开 B02-002 草稿并返回后，清单仍可见；没有搜索框、地图筛选或保存视图入口。
- 仅选中本任务无 Run 的 B02-002 草稿归档：提示“1 个实验已归档”，当前清单从 9 变为 8。切换“已归档”能看到该草稿，负责人“当前研究员”、标签“B02-002 / 界面回归”均保留。恢复后提示“1 个实验已恢复”，归档清单为空；切回“当前实验”并刷新，仍为 9 个、第一页 5 条，元数据保持。
- 本轮没有删除真实实验、修改实验定义或操作用户其他 Run。打开、归档和恢复经过浏览器验证；新建、复制、删除及 Run 相关控制的代码合同由工程回归覆盖，没有在本轮重新执行所有业务路径。
- B02-003/004 按“相关功能已移除”关闭，不宣称已完成被取消的搜索和保存视图功能验收。

## 另行报告的现象

- 重启后的首次列表加载在“正在加载实验…”停留约一分钟后正常完成；后续翻页、返回、归档恢复及刷新均加载成功。工程子 Agent 只读核查重启后的日志尾部：正常启动，检查范围没有 ERROR/Traceback/Exception，当前/归档列表请求均为 200，未见被删入口的请求或对应 404/500。初始化直接加载实验，列表请求只有 page、固定 page_size=5、archived，没有被删功能依赖。日志不含耗时，尚不能解释首次等待；未擅自修改性能逻辑。
- 在重启前只读打开“溪谷大学四人两日生活”的结果页时，回放提示“地图或人物资源加载超时”，地图检查器显示“回放加载失败”。用户随后明确指出只是其他实验加载慢、要求聚焦案例 2。未安排修复，撤销此项阻断；详见 `blockers.md` 的 B02-005 历史观察。
