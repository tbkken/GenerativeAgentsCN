# 案例 2 阻断记录

**当前停点（2026-09-09 21:11，Asia/Shanghai）：B02-007，案例2封门对照的结果页明确提示“地图或人物资源加载超时”。按用户规则停止后续操作，等待判断；未授权、未实施工程诊断或修复。** 正式8步和视野/注意力对照已有完整导出，封门对照的实际MCP已读到，但回放与导出验收尚未完成。

## B02-007：当前案例封门对照的回放资源加载超时

- **状态**：实际页面卡点，根因未确认，等待用户评估。未把它归因于B02-006修复，也不扩展检查其他实验。
- **当前实验**：`37378809-78d2-45c2-a4b6-5653f0f44142`，案例2对照：封闭门洞后的阅读桌可达性。
- **Run / Step**：`0f4f7968-bbd3-4812-aed9-acd3ccf6b6e7`，已完成1/1步，UI耗时26秒。Attempt01曾在模型页显示，但完整Attempt ID未在停点前抄录，不猜测补填。
- **停点入口**：`http://127.0.0.1:8000/?experiment_id=37378809-78d2-45c2-a4b6-5653f0f44142&view=results&run_id=0f4f7968-bbd3-4812-aed9-acd3ccf6b6e7&result_tab=artifacts`。
- **操作过程**：等待本例运行完成→查看仿真诊断/模型调用（详情慢加载后成功返回）→进入仿真回放→等待角色与帧加载→点击“导出结果”并查看“结果与导出”→继续等待并切换本Run的质量/回放/导出页。没有重跑、重启、编辑运行包或处理其他实验。
- **实际观察**：回放曾长时间停在`Step 001 / 1`、播放/滑块禁用、角色列表显示“正在读取Agent…”；之后名单出现林晨，但检查器仍无完整帧。事件文字可见，不据此声称地图和角色画面已验收。模型详情的3次真实MCP已成功读到，运行也已完成；这不属于模型尚未生成结果。
- **错误原文（浏览器AX）**：`操作失败`；`发生了什么：地图或人物资源加载超时；影响：当前操作没有完成；如何修复：检查当前配置后重试；如仍失败，请复制诊断信息交给开发人员。`
- **导出状态**：触发一次“导出结果”后，首次停点页面仍只有自动`quality-report.json`，显示“1个可用 · 0个构建中或失败”，当时未看到新任务编号或新ZIP。停止操作、保留标签页时，异步反馈随后显示“制品任务`7d2a3e65`已完成”，列表仍是此前单份报告。故不能将资源超时断言为导出后端失败；尚未刷新下载及核验，不宣称已归档。未重复提交导出任务。
- **证据范围**：本会话AX保留上述错误原文；随后截图保留本Run已完成1/1和仅一份报告的结果列表，截图时短暂错误toast已消失。未修改截图或伪造文件证据。
- **影响**：当前案例封门对照的回放画面与原始导出无法完成验收。主正常Run与边界Run已有的核验结论不受此观察推翻；公共地图两格门洞已恢复，并在编辑器重新确认可达11格。
- **待用户决定**：是否批准仅针对本Run的回放资源超时做诊断；确认原因后再决定修复。需区分资源实际缺失/错误、服务响应慢和前端加载/取消/超时处理，不预设实现方案。导出是否受同一原因影响需要独立核实。
- **后续验收条件**：获准处理后重回本Run原路径，完整显示地图、林晨和Step1实际(7,7)；只读导航仍返回不可达且无移动。最终导出任务能完成并下载原ZIP、质量报告，再以下载副本核验门洞两格差异、世界事实与包内资源。未经批准不启动工程子Agent。

日期：2026-09-09。案例 2 正式双房间地图、角色、人群、Brain 与 8 步 × 1 分钟参数已保存。**B02-006 已修复并通过原界面保存、重开、预检及封存复验；初始 (7,7)/门廊已生效，正式正常流程已 COMPLETED 8/8。** Run `b4b4ca09-87bd-4980-bdc5-8146474d6225`，唯一 Attempt `881415da-972b-4831-8df5-a2ede8d62bcb`；正常流程和导出证据见 [正式运行记录](formal-run-record.md)、[导出核验](exports/README.md)。边界对照仍在进行，完整案例尚未标记 PASS。实验中心列表精简已验收，B02-003/004 按功能移除关闭；B02-002 已通过。B02-001 的工程修复、14 项针对性测试和有效浏览器复验已通过，详见原始及严格顺序副本记录。B02-005 经用户确认只是其他实验加载慢，不作为阻断或诊断修复任务。

## B02-001：已感知 Game Object 的地址未纳入导航知识校验

- **状态**：已修复并通过有效浏览器复验。按既有批准完成真实复现与最小修复，14 项针对性测试通过。原封存实验两次复验因模型抢跑猜目标而无效；通过 UI 在独立副本加强当前目标的依赖顺序说明后，同一真实料理台地址导航成功，角色不移动。原始失败和无效复验全部保留，详见 `b02-001/browser-run-record.md`，不能称所有 Run 都通过。
- **预期**：Agent 从合法感知结果取得当前视野内 Game Object 的完整四层地址后，可以把该地址提交给只读导航；是否可达另由碰撞与路径检查判断。感知与导航应遵守同一 Agent 的范围及注意力限制。
- **发现**：`generative_agents/runtime/capabilities.py` 的 `_navigate` 对不在当前地址、也不在 `agent.spatial.tree` 中的目标，只查询 `_perceive({}).spatial_nodes`（约 373–377 行）。同文件的感知结果构建明确从 `spatial_nodes` 排除 Game Object（约 450 行），对象另放入 `game_objects`。因此该条件下四层对象地址无法通过这项感知知识校验，可能被拒绝为 `navigation target is not perceived or remembered by this Agent`。
- **影响范围**：案例 2 中“先感知对象，再按完整对象地址导航”的分支；不是所有坐标导航或三层 Arena 导航都会失败。不能用预置全图地址、改用坐标或只验证 Arena 分支掩盖该缺口。
- **已批准的最小复现**：通过 UI 准备一个可走、可感知但未预置到已知空间树的非当前 Game Object；记录其在感知 `game_objects` 中的完整地址，再提交同一地址导航。目标不在当前位置，且需位于本轮真实可见候选内，以排除视野、注意力、地址格式和家具阻挡等混淆。
- **已批准、待复现确认后实施的修复方向**：导航知识校验接受当前 Agent 实际感知返回的空间节点和对象地址，同时保留视野、注意力、已知空间及可达性边界；不得为了接受地址而扩大可见范围。
- **验收建议**：可见且入选的对象地址可导航；被范围/注意力排除且未记住的地址仍拒绝；导航调用本身不移动；门洞堵塞仍按正常不可达结果返回。

### B02-001 已完成的浏览器准备

- 新建专用 Brain：`77f54ecd-84de-4215-9d59-e64270404688`，key 为 `book-case02-visible-object-navigation-check`，保存后内容哈希 `c563de96fe3f`；UI 确认仅引用 `world-perceive`、`world-navigate`、`world-act` 三个公共 MCP，无私有脚本。
- 新建诊断实验：`837e7a6f-885f-489f-8074-2d20ec6acb1b`，名称“B02-001：已感知对象地址导航·修复前复现”。地图“陈明远的家·背景版”和“案例1：陈明远晨间单Agent”只用于最小工程诊断，不是案例 2 正式地图或人物。
- 初建时曾为默认 1000 步、10 分钟的 DRAFT，后经 UI 改成 1 步、1 分钟，出生 `(9,5)`，确认已知空间只有卧室/床边，并核验可走路径后封存。修复前 Run `6f9c4463-94af-48d9-a4fd-855108249435` 准确复现导航拒绝。严格顺序副本的 Run `fc9dcaf0-fd0e-480b-ae99-a0a2d90f7e86` 通过实际导航与只读验证，未执行业务评估。
- 诊断 Brain 精确正文另存于 `b02-001/diagnostic-brain.md`。
- 曾临时复制案例 1 回归实验，生成草稿 `8b97b220-9efe-4325-bbc9-06c1ae18d3a3`；因需选择新 Brain，已在 UI 删除这个本轮刚创建、没有 Run 的副本，再走新建实验向导。没有删除案例 1 的原始实验或 Run。

## B02-002：新实验顶部残留已删除副本的完成反馈

- **状态**：用户批准后，由工程子 Agent `fix_b02_002` 修复，41 项前端测试通过，Web 重启成功，主 Agent 浏览器原路径复验通过。
- **入口**：`http://127.0.0.1:8000/?experiment_id=837e7a6f-885f-489f-8074-2d20ec6acb1b&view=overview`。
- **复现过程**：在案例 1 的 P01–P03 回归实验中点击复制 → 删除本轮新建的无 Run 副本 → 创建诊断 Brain → 通过新建实验向导创建 B02-001 实验 → 查看实验概览顶部。
- **预期**：旧实验的复制完成反馈在离开该操作上下文后清除；新实验页面的反馈应属于当前实验或当前操作。
- **实际**：新实验标题、草稿徽标与左侧“实验结果 0”已正确切换，但“复制实验”按钮旁仍显示绿色文本“已完成：案例1：P01–P03 修复回归（6步） 副本”。这个副本此前已被删除。随后截图再次确认仍显示相同旧文本，不是只读树在切换瞬间捕获的单次现象。
- **证据**：本会话浏览器 AX 树及截图。截图同时显示当前 B02-001 标题、草稿徽标和上述旧完成文本。该文本属于操作反馈；目前不将其误报为真实 Run 状态或数据库事实错误。
- **影响**：旧实验操作反馈跨页面残留，容易误导用户对当前实验操作结果的理解；尚无证据表明它改变实验数据或仿真状态。
- **已批准的修复范围**：工程子 Agent 定位顶部操作反馈的作用域；在实验切换、删除和新建后清理旧反馈，防止旧异步完成覆盖当前页面，保持有用的当前操作提示。
- **验收建议**：重走以上路径，新实验不显示已删除副本反馈；当前实验保存/复制操作仍显示正确反馈；切换实验时旧操作响应不覆盖新上下文。

以下为 B02-002/003/004 的历史准备与验证记录，最新状态以上方各项状态及本文末尾为准。合同核查子 Agent 仅完成 B02-001 的只读方案；资料准备子 Agent 仍中断。正式案例素材仍只有 `agents/林晨/assets/` 的两张本地图片，尚未上传或绑定系统资源。

### B02-002 复验准备

- 用户同意后的浏览器清单没有原标签页，已新开标签页进入诊断草稿。首次全新加载时旧反馈未出现；这只符合内存残留的特征，不能证明修复通过。修复后必须重新执行复制、删除、离开工作区、新建实验路径。
- 重启前通过 UI 清除列表筛选并查看运行中项目：“溪谷大学四人两日生活”，Run 前缀 `110b4ddb-902`，显示 514/576 运行中。仅做重启影响核对，不把本项无关仿真作为验收对象。
- 已核对 `restart-web.bat` 只停止端口 8000 的 Web 入口进程，保留仿真与制品工作进程。修复测试通过后由主 Agent 执行该已授权脚本重启。

### B02-002 修复与复验结果

- 修改文件：`generative_agents/web/static/console-api.js`、`tests/frontend/duplicate-feedback.test.cjs`。为复制反馈增加独立 generation；进入全局中心或打开其他实验时清除旧提示和重试目标；已发出的复制请求在旧上下文完成时仅记录操作历史，不覆盖新页面或抢回旧副本。
- 工程验证：新增 6 项回归；全部前端测试 41/41 通过；`node --check` 和 `git diff --check` 通过。未修改任何真实实验/Run 数据或 B02-001 导航代码。
- 主 Agent 执行 `restart-web.bat`，退出码 0、健康检查成功，Web PID 从 54596 更新为 40016。
- 浏览器从原案例 1 回归实验 `56ff8cc0-ecae-4691-bf7d-ab4910f98b79` 复制出本轮无 Run 草稿 `8288edeb-ddf2-4f36-a51c-276d901acd10`。复制后当前副本正确显示“已完成：案例1：P01–P03 修复回归（6步） 副本”。
- 在 UI 删除该本轮副本，进入大脑中心，再通过新建向导创建“B02-002：复制反馈作用域回归”：`41c2649d-a3a9-4a02-9f8f-168aa6e11239`。概览正确显示其名称、草稿、实验结果 0，顶部没有旧复制反馈。这个仅用于界面回归的草稿没有执行仿真。
- 点击“保存实验定义”，得到当前操作的“实验定义已保存 / 实验名称、目标与故事已更新。”提示；当前正常反馈保留。AX 树及截图均确认无旧副本完成文本。
- 原路径中的 Brain 创建已在上轮完成，本轮复验访问大脑中心并复用该诊断 Brain，没有多建重复 Brain。延迟成功/失败与自动打开副本详情期间切换由新增回归测试验证，未声称在生产页面人为制造网络延迟。

## B02-003：聊天模型名称进入地图筛选，返回列表后实验全部被隐藏

- **状态**：已通过临时内存诊断取得浏览器原生 autofill 证据；用户随后明确要求删除整个搜索筛选功能，现已完成前后端移除和列表浏览器验收，按功能移除关闭。临时诊断已清理，不是“保留筛选功能并修复通过”。
- **入口**：实验中心 `http://127.0.0.1:8000/`，由实验概览左侧“实验”进入。
- **本轮证据链**：先在 UI 清除筛选；重启 Web 后刷新列表，AX 树明确显示地图筛选为空、全部 8 个实验。随后执行 B02-002 原路径复验：打开案例 1 回归实验 → 复制 → 删除本轮副本 → 大脑中心 → 新建 B02-002 界面回归实验 → 保存实验定义 → 返回实验列表。上述操作中没有填写地图筛选，也没有加载保存视图。
- **实际**：返回列表后，`experimentMapFilter`（“地图筛选”）值为 `Qwen3.8-27B-UD-Q4_K_XL`；“模型筛选”为空；顶部统计全部为 0，正文“没有符合条件的实验”。AX 树与截图一致。该名称是创建向导中选择的聊天模型，不是地图名称。
- **预期**：实验的聊天模型配置不应修改实验列表的地图筛选；仅用户主动设置筛选或载入明确的保存视图才应改变该条件。列表应按原来的空筛选展示现有实验。
- **影响**：返回列表后找不到刚创建及已有实验，用户容易误判资源丢失。当前证据只说明筛选误入值导致列表被隐藏，不说明实验数据已删除。
- **范围与证据更新**：不需要创建、复制、删除或保存，单纯加载后也可出现。09/09 16:05 操作历史中的临时诊断显示原生 input/change 的 `isTrusted:true`、`autofill:true`，DOM observed change 从空字符串变为模型名称，随后 `reloadExperimentFilters` 把该值采入 `state.mapFilter`；未出现应用代码 DOM value setter 写入堆栈。不能将此现象归因于 B02-002 或 B02-004。
- **已批准的修复范围**：定位聊天模型配置与实验列表筛选的 DOM/状态边界，防止跨表单写值，保留用户主动输入的地图、模型筛选和保存视图功能。
- **验收建议**：空筛选下打开/创建/保存实验并返回列表，地图筛选仍为空、资源可见；人为设置地图筛选和模型筛选各自有效且互不串值；明确载入保存视图仍按其条件显示。
- **当时停点（历史）**：B02-001 原实验曾尚未恢复配置、没有 Run，不能直接运行默认 1000 步草稿。后续已改成 1 步并完成真实复现，见本文 B02-001 当前状态。

### B02-003 获批后的最小浏览器复现

- 新标签页初次加载实验中心，地图和模型筛选均为空，全部 9 个实验。
- 仅点击已有草稿“B02-002：复制反馈作用域回归” (`41c2649d-a3a9-4a02-9f8f-168aa6e11239`)，待概览显示后直接点击左侧“实验”。没有编辑、保存或其他系统写入。
- 返回列表后 `experimentMapFilter` 已为 `Qwen3.8-27B-UD-Q4_K_XL`，模型筛选为空、全部 0。由 AX 树确认。主 Agent 随后点击清除筛选，列表又显示全部 9，准备检查重启影响；在工程子 Agent 报告 B02-004 后立即停止。

## B02-004：清除或载入筛选视图时，配置参数遮蔽 DOM document

- **状态**：历史工程修复和清除筛选浏览器验证已完成；用户随后明确删除搜索及保存视图功能，现已完成相关函数、入口和专用接口移除及列表浏览器验收，按功能移除关闭。下述 44/44 是当时修复的历史结果，不代表删除后的测试数量。
- **发现位置**：`generative_agents/web/static/console-api.js:4033` 定义 `applyListViewDocument(document)`，参数实际是普通筛选配置对象。`4055` 行调用 `document.querySelectorAll('.filter-tab[data-filter]')`，这里的 `document` 仍指该参数，遮蔽了全局 DOM document；普通配置对象没有该方法，会抛出 TypeError。
- **调用路径**：`4542` 行清除筛选传入 `{}`；`4569` 行下拉选择保存视图传入 `saved.query`；`5264` 行共享视图初始化传入 `savedView.query`。
- **预期**：清除或载入视图完整更新筛选字段、状态标签，并继续刷新列表；共享视图初始化应能继续加载页面。
- **代码推导的实际影响**：异常发生前，筛选状态及输入框已被修改；异常后，清除筛选的选中项清理/列表刷新、保存视图的列表刷新及共享视图后续初始化不能按该调用链继续。其他异步刷新仍可能让列表稍后显示，因此不能把曾观察到“清除后列表恢复”当作完整路径没有报错的证据。
- **与 B02-003 的关系**：同属筛选功能，但此异常不能解释聊天模型名称自动写入地图筛选，按独立问题登记；不扩大 B02-003 授权去静默修复。
- **已批准的修复范围**：区分配置参数和全局 DOM document，恢复筛选视图完整执行链；增加清除、载入及共享视图初始化的必要回归，验证用户选择的筛选仍保留。
- **验收建议**：清除筛选后字段、选中项、状态标签及列表同步；已保存视图和共享链接正确应用条件并加载实验；无该 TypeError。保留 B02-003 原最小复现，单独验证其模型串值是否修复。

### B02-004 工程修复与当前浏览器证据

- `generative_agents/web/static/console-api.js` 中配置参数改名 `viewDocument`，DOM 查询保持全局 `document`。
- 新增 `tests/frontend/experiment-list-view.test.cjs` 三项回归：清除后字段/选中项/列表同步、保存视图地图与模型条件独立、共享视图完整初始化。修复前先复现 TypeError，修复后 44/44 前端测试通过；语法及空白检查通过。
- 主 Agent 运行已授权 `restart-web.bat`，健康检查成功，Web PID 40016 → 37344；未创建或恢复任何 Run。
- 浏览器先选中“B02-002：复制反馈作用域回归”，再搜索不存在的回归词，确认空结果和“比较 1”；点击“清除筛选”后检索词及地图条件清空、“比较 0”、批量操作禁用、全部 9 个实验显示。该路径核验了原异常之后的选中项清理及列表刷新。
- 当时尚未保存回归视图：保存预览被 B02-003 自动写入模型名，已取消，不保留错误视图。后来用户删除整个保存视图功能，剩余保存、载入及共享链接验收随之取消。

B02-003 同项新证据：全新标签尚未打开任何实验时，仅点击“草稿”再打开保存视图，预览已出现错误 `map_key`；不能将“打开概览”说成唯一触发条件。其后原生 autofill 证据见上文；B02-001 与正式案例 2 构建均未继续。

## 用户最新决定：精简实验中心列表

用户明确要求彻底删除比较、添加标签、转移负责人、搜索功能、页容量选择、紧凑表格、保存/已保存/删除视图、全部/草稿/已封存/最近仿真状态计数整行及对应搜索框，并清理相关逻辑与空白布局。列表固定每页 5 条。

- 已交工程子 Agent `fix_b02_003` 实施前后端专用逻辑清理；没有继续实施 autofill 兜底或保留保存视图功能。
- 保留实验新建、打开、复制、删除、归档/恢复等基本管理及翻页；现有归档状态下拉作为访问归档资源的管理入口保留，默认仍显示当前实验。实验本身 owner/tags 元数据不因删除批量入口而被删除。
- 当前状态：已完成。45/45 前端测试、7 项针对性 Python 测试通过，Web 重启成功；浏览器确认固定 5 条分页、打开返回、归档恢复及元数据保留。详细验收记录见 `experiment-list-simplification.md`。历史缺陷证据保留，以“相关功能移除”关闭 B02-003/004。

## B02-005：现有 Run 回放的地图或人物资源加载超时

- **状态**：用户明确判断只是加载慢，并指出其他实验不属于当前任务。撤销案例 2 阻断，不诊断或修复该实验；本项只保留历史观察，不作为已确认系统缺陷。
- **入口**：实验“溪谷大学四人两日生活” → “查看仿真” → “仿真回放”。URL：`http://127.0.0.1:8000/?experiment_id=cb1fef88-b395-414e-941c-49a0aac7cbd6&view=results&run_id=110b4ddb-9022-4022-b368-e4ded170f72f&result_tab=timeline`。
- **上下文**：为已授权的 Web 重启做只读状态检查，在重启之前打开该结果页；未编辑实验、未暂停/恢复/取消 Run。页面显示所选 Run 为“运行中 · 514/576 步”，回放停在 Step 001 / 576；Attempt 未在当前页面显示，未进一步查询。
- **预期**：打开已有 Run 时能显示其地图、人物及可回放的已提交帧。
- **实际与证据**：浏览器 AX 树显示 toast“操作失败”，正文“发生了什么：地图或人物资源加载超时；影响：当前操作没有完成；如何修复：检查当前配置后重试；如仍失败，请复制诊断信息交给开发人员。”检查器动作项为“回放加载失败，请查看上方诊断”，Agent 列表仍为“正在读取 Agent…”，回放上一步/播放/下一步禁用。事件文本能显示，不能据此声称地图与人物已成功加载。
- **影响与界限**：当前这次打开无法检查回放画面。只有单次 UI 失败证据，尚未确认根因、持续性或与本次列表清理的因果关系；也不能仅依据 514/576 的状态快照断言 Run 卡死。
- **后续处理**：不再推进原先提出的其他 Run 诊断建议。聚焦案例 2 自身的资源、配置和运行；加载中应给页面合理完成时间，不能把其他实验的单次慢加载升级成当前案例阻断。

## B02-006：真实门廊空地的初始地址被强制要求包含 Game Object

- **状态**：用户批准后已完成工程修复、29 项针对性测试、Web 重启与原界面保存/重开/预检/封存复验，B02-006 闭环。下列原错误与影响作为历史证据保留；后续正式正常流程已完成 8/8 步，边界对照仍另行验收。
- **入口**：正式实验“案例2：穿过门口，到阅读角看书”，`c63b2d81-433a-4c2f-9d75-b44bcf04e318` → 参与 Agent → 编辑林晨 → 初始位置与空间。
- **角色**：本案例独立 Agent `agent-mttvoove-oxit70`。两张图片已复制进实验，显示 2.5 格、视野 10、注意力 6。系统创建时初始坐标为 (3,4)，四层地址为“林晨的门廊与阅读室 > 馆内阅读空间 > 门廊 > 置物边柜”。
- **复现步骤**：将初始 X/Y 改为 7/7；界面显示“当前坐标的地图语义：林晨的门廊与阅读室 > 馆内阅读空间 > 门廊”；点击“同步为初始位置”，常用地址中的初始位置随即变为相同三层地址；点击“保存 Agent”。
- **实际错误原文**：`发生了什么：Agent agent-mttvoove-oxit70 needs one four-level initial_location；影响：当前操作没有完成；如何修复：检查当前配置后重试；如仍失败，请复制诊断信息交给开发人员。`
- **修复前证据**：本会话 CUA AX 树明确记录操作失败及同步后的三层地址；当时截图显示 7/7、地图语义和初始位置字段一致，仍停在未保存的编辑弹窗。该次填写不能记为持久保存成功。
- **预期**：坐标位于 Arena 内、不属于任何 Game Object 的正常可走空地时，可以保存地图实际返回的地址。固定四层地图结构不应意味着每个可走格都必须被一个 Game Object 覆盖；真实地址仍须与坐标一致。
- **修复前影响**：本例预定起点正是门廊空地 (7,7)，此前地图两点路径检查已确认至 (16,7) 可达 11 格；保存失败使本例不能从该起点运行。根因在获批后的工程定位中确认，见下文。
- **已批准的修复范围**：统一初始位置保存与实验预检的空间地址合同，让真实 Arena 空地可作为起点，同时继续校验稳定层级、真实坐标、碰撞与对象存在性；不伪造第四层、不放宽导航感知边界。修复完成再由主 Agent 重走同一 UI 路径。
- **继续验收的条件**：同一实验将 (7,7) 与其真实地址成功保存、重新打开仍一致，原有真实四层对象起点仍可保存，错误地址及不可走起点仍被拒绝；随后才开展正式 8 步 Run 与画面、事实和导出验收。

### B02-006 工程修复与浏览器复验结果

- 实际保存链为 `PUT /api/studio/experiments/{id}` → `ExperimentWorkspaceService.replace_definition` → `validate_experiment_directory`；预检和封存共用这一协议校验。`ga_protocol/validation.py` 原先强制 `initial_location` 长度为 4，现接受真实 3/4 层地址，仍与出生 Tile 完全一致并存在于 Agent 空间树，未放宽为地址前缀匹配。
- `ga_studio/workspace.py` 的可走出生候选及空间树构造同步接受 Arena/对象地址；Arena 的物件列表可以为空。`config/spatial.py` 本次仅将“四层初始位置”文案改为“真实空间地址”，没有修改该文件既有空间重构。
- 新增 `tests/test_initial_location_contract.py` 的 17 项回归。修复前选跑 4 项，三层保存和导入失败、四层通过，结果 **2 failed / 2 passed**；保存错误与浏览器相同。修复后连同原物理复制、三层 Tile 和 B02-001 导航回归共 **29 passed（21.25 秒）**；编译与范围内差异检查通过。
- 回归覆盖三层/四层保存、重开、预检与封存；错误或截断地址、未知 Arena/对象、非法深度、空层、缺失空间树、碰撞/越界出生点保持拒绝；语义节点 ID、种类和父层仍校验，失败保存保持原实验包。测试仅使用临时数据。
- 主 Agent 执行已获授权的 `restart-web.bat`，Web PID **40368 → 42456**，健康检查通过。
- 在同一实验 `c63b2d81-433a-4c2f-9d75-b44bcf04e318`、同一 Agent `agent-mttvoove-oxit70` 的原编辑弹窗，点击“同步为初始位置”→保存，UI 提示 **“Agent 已保存”**，列表显示 **7,7**。重开编辑仍为 **(7,7)** 与三层 **林晨的门廊与阅读室 → 馆内阅读空间 → 门廊**；已知空间仅同一门廊，物件列表为空。
- 原实验预检为 **0 阻断、0 警告、0 自动检查、1 通过**，随后封存并启动正式 8 步 Run：`b4b4ca09-87bd-4980-bdc5-8146474d6225`，Attempt `881415da-972b-4831-8df5-a2ede8d62bcb`。启动时曾记录 **1/8 运行中**，后续终态已核对为 **COMPLETED 8/8**；实际三次 MOVE 为 4+4+2 格，抵达后才 ACT。门洞及桌旁回放、8 帧事实和原始导出已核验；33 次逻辑调用、33 次物理尝试无重试，36 次 MCP 无拒绝，唯一质量项为 Step 1 首次空记忆检索。
- 三层真实起点的原 UI 路径已通过；四层起点和非法输入边界由本轮工程回归覆盖，没有为浏览器反向验证而改动当前正式实验。B02-006 通过不等于完整案例 PASS。
