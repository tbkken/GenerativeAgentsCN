# B02-001 浏览器最小复现记录

日期：2026-09-09。状态：修复前已复现，工程修复与 14 项针对性测试完成；修复后严格顺序副本的有效浏览器验证通过。原实验的两次无效复验完整保留，不等于同一原始提示条件下全部运行通过。

本实验只诊断案例 2 需要的“已感知对象完整地址导航”能力，不是正式双房间阅读案例。所有系统配置及执行均经浏览器完成。

## 身份与参数

- Experiment：`837e7a6f-885f-489f-8074-2d20ec6acb1b`，B02-001：已感知对象地址导航·修复前复现。
- Run：`6f9c4463-94af-48d9-a4fd-855108249435`。
- Attempt：`5db36c2b-e24d-45dc-aa34-c5cb2f2bc26c`，Step 1；COMPLETED，1/1，界面墙钟 31 秒。
- Agent：`agent-mtsv5yh2-nwzlt5`，陈明远；初始坐标 `(9,5)`，显示大小 3 格，视野 10，注意力 6。
- 已知空间仅 `陈明远的家·背景版 > 住宅 > 卧室` 的 `床边`。没有预填厨房、料理台或全图地址。
- 初始时间 `2026-09-09T09:00:00+08:00`，1 步，每步 1 分钟，种子 42，检查点频率 1、保留 2、投影频率 1，开启模型 Payload 留存。
- Brain：`book-case02-visible-object-navigation-check`，精确正文见 `diagnostic-brain.md`。
- 当前目标改为“仅检查眼前一个已感知但未预先记住的对象地址能否用于只读导航；如实保留工具返回，本轮始终站在原地。”

## 前置条件的界面证据

地图编辑器只读检查：料理台范围 X=4、Y=12、W=8、H=6；两点路径检查 `(9,5) → (10,15)` 返回“可达 · 21 格”，目标显示“可走 / 素材结果”。没有编辑公共地图。

实验参与 Agent 编辑器确认出生坐标对应 `陈明远的家·背景版 > 住宅 > 卧室 > 床边`，可用空间只有卧室/床边。执行前预检为 0 阻断、0 警告。

## 同一 Agent、同一步的实际调用

来源：结果 → 仿真诊断 → 模型调用 → 第三次物理请求（序号 8）→ 页面展示的 Payload 与 MCP 记录。不是直接读取后台接口。

1. `world-perceive({})` 成功。返回坐标 `[9,5]`，实际半径与上限均为 10；`game_objects` 包含料理台，键 `game_object-mtsycz2l-rrsw9c`，距离 7，完整地址 `['陈明远的家·背景版','住宅','厨房','料理台']`。注意力带宽 6，`truncated:false`。
2. `world-navigate` 使用上述原始 `target_address`，没有传 `target_coord`，没有缩短地址。返回 `isError:true`，错误为 `MCP error: navigation target is not perceived or remembered by this Agent`。
3. `world-act` 提交 `ACT / 检查路线 / 料理台`，description 如实记录拒绝。返回 `accepted:true`，实际 `target_coord:[9,5]`，地址仍为卧室/床边，`path:[]`。没有 MOVE，没有追加记忆，也没有第二次导航换目标。

共 3 次逻辑调用、3 次物理请求，重试 0；界面行为质量计数 1。COMPLETED 表示单步执行结束，不能解释成导航能力通过。

## 后续

已按用户针对 B02-001 的既有批准交工程子 Agent 最小修复：`capabilities.py` 在同一次 `_perceive({})` 的 `spatial_nodes + game_objects` 中精确匹配完整地址；保持视野、注意力、已知范围与寻路合同。

新增 `tests/runtime/test_visible_object_navigation.py` 的 10 项测试，修复前 3 failed / 7 passed，三项失败均复现同款导航拒绝；修复后连同原导航/感知相关回归共 14 passed。覆盖可见对象、视野/注意力排除、同 Arena 不等于知道对象、已记忆对象、堵门不可达、同名不同地址、当前位置与只读。编译及范围内 diff 检查通过。

主 Agent 执行 `restart-web.bat` 成功，Web PID 53552 → 40368。浏览器随后重跑同一封存实验，保留原失败 Run，不用另一个配置冒充原路径通过。

## 修复后的两次无效复验

同一封存实验先后产生 Run `6284ca74-03e2-4af1-a395-0ccde5ea0631`（Attempt `bf758e92-99b0-43cc-a2d0-c9188bee3014`，21 秒）和 `3902cb28-cd9c-4a30-8591-69c901bad0a1`（Attempt `27759f41-51f7-47d2-91e4-e20cac72e7b7`，19 秒）。两者均完成 1/1，但不是有效正向复验：模型在首次响应同时发出感知和导航，把从房间描述猜出的“卧室/床头柜”当目标，没有等感知工具返回。该对象不在可见候选或已知树，系统正确拒绝。

随后 ACT 描述错称调用了料理台，实际 MCP 输入仍是床头柜。必须以工具输入为准，不能根据这段自然语言判定料理台仍被拒绝。两次均 2 次模型调用、0 重试、1 个质量计数，全部保留；没有因这类模型偏离修改 Runtime 或扩大导航权限。

## 严格顺序副本的有效复验

通过 UI 复制为独立实验 `b045242b-c048-4cd4-8101-d3d75ecf4f10`，命名“B02-001：对象地址导航·严格顺序复验”。地图、Brain、出生点、空间记忆、视野、注意力、步数和模型保持复制所得内容；只调整说明和 Agent 当前目标，让依赖顺序更明确。当前目标精确文本：

> 本轮只检查路线，不移动。第一条响应只发出一个 world-perceive({}) 工具调用，然后停止本次输出，等待它的真实返回；此时不请求导航、不预写 target_address。收到结果后的下一条响应，才从实际 game_objects 选择一个非当前、未记住的对象，原样复制其四层 address，单独调用 world-navigate；不从空间描述中的家具名称猜对象。再次等待导航返回，最后才单独调用 ACT 检查路线，准确记录实际调用的对象与返回值。每条模型响应只有一个工具调用，三次调用必须由真实返回分开。

- Run `fc9dcaf0-fd0e-480b-ae99-a0a2d90f7e86`，Attempt `96eb332a-c206-4f23-a4e2-2e21fdc385b0`，Step 1，COMPLETED 1/1，21 秒。
- 浏览器真实 MCP：先 `world-perceive({})` 返回同一料理台、距离 7、视野 10、带宽 6、无裁剪；接着独立 `world-navigate` 原样提交 `['陈明远的家·背景版','住宅','厨房','料理台']`，返回 `reachable:true, distance_tiles:19, next_coord:[10,5], movement_required:true`。
- 随后独立 `ACT / 检查路线 / 料理台`，`accepted:true`，实际坐标仍 `[9,5]`、卧室/床边，`path:[]`。未写记忆、未提交 MOVE、未用坐标绕过地址检查。
- 3 次逻辑调用、3 次物理请求，0 重试。行为质量页显示“基础诊断未发现告警；本次未执行业务评估”。
- 对象地址导航自动选择对象范围内的可走目标，因此其 19 格与人工指定 `(10,15)` 的 21 格不是同一终点比较；不能把此差值误报为路径计算错误。

结论：真实可见且未预先记忆的同一对象完整地址，修复后可以导航；只读查询仍不移动。此项通过不等于正式案例 2 已运行，也不掩盖前两次模型依赖顺序偏离。
