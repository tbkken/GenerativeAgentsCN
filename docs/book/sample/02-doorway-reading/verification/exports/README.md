# 案例 2：浏览器导出结果与下载副本核验

核验日期：2026-09-09。本目录保存主 Agent 经浏览器“导出结果”和“下载”取得的原始副本。核验只读取这两个下载文件；内嵌实验曾解包到本案例 `verification/` 下的临时目录执行纯文件协议校验，临时目录已移除。没有读取原 Run 工作目录、数据库，没有调用系统接口或执行 Runtime，也没有修改原 ZIP 和 JSON。

## 原始文件

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| [结果审计 ZIP](result-bundle-step-000008-31f5c03d.zip) | 2,482,114 | `0858170246f449c7dc5bd787fe95856717c698e05be0796af17ba19aafb59379` |
| [完整质量报告](quality-report-step-000008-963c8f78ba263489-bffcd9dc.json) | 985 | `963c8f78ba26348901aaffa9a465eafecbb72a32428f0f13c04b579844e26518` |

两文件大小、哈希均与浏览器下载后归档记录一致。ZIP 共 74 个成员，未压缩总量 4,792,805 字节，无重复成员，CRC 校验通过。ZIP 内的质量报告与独立 JSON 逐字节一致。包内该质量报告的来源记录为 `COMPLETED`、Step `8/8`、`partial=false`，内容哈希及大小相符，生成时间为 `2026-09-09T12:41:38.577389Z`（北京时间 20:41:38）。

## 身份、参数与自包含素材

- 实验：`c63b2d81-433a-4c2f-9d75-b44bcf04e318`，名称“案例2：穿过门口，到阅读角看书”。
- Run：`b4b4ca09-87bd-4980-bdc5-8146474d6225`，状态 `COMPLETED`，提交 `8/8`。
- Attempt：`881415da-972b-4831-8df5-a2ede8d62bcb`，只有一次 Attempt，从 Step 1 连续提交至 Step 8，无恢复、无失败。
- Attempt 起止：`2026-09-09T12:32:58.721675Z` 至 `2026-09-09T12:38:46.996380Z`，相差 **348.274705 秒**，对应页面 **5 分 48 秒**。

内嵌实验的完整性清单覆盖 **13 个文件**，逐文件 SHA-256、文件集合和实验协议校验均通过；Run Manifest 引用的实验 ID 与根哈希一致。内嵌实验根哈希为 `739e5fd33b0ee0f5af985f193790ffcafb2d0402388f7d1a4b61b6d89a106101`。

| 内容 | 下载包中的实际配置 |
| --- | --- |
| 地图 | “林晨的门廊与阅读室”，24 × 16 格，Tile 32；坐标范围 x=0–23、y=0–15，共 384 Tile。包内 `definition.size` 原始顺序为 `[16,24]`，不是 x/y 上限。 |
| 图片 | 独立背景 PNG 1536 × 1024；林晨头像 32 × 32；行走图 96 × 128。三文件均在内嵌实验 `assets/` 中，Agent 和地图引用指向包内文件。 |
| 人物 | 林晨，21 岁，`agent-mttvoove-oxit70`；初始 `[7,7]`，门廊；显示 2.5 格，`sprite_layout=4x3`；视野 10，注意力 6，感知模式 `box`。 |
| 时间与运行 | `Asia/Shanghai`；开始 `2026-09-09T09:00:00+08:00`；每步 1 分钟，共 8 步；随机种子 42。 |
| 记录 | 每步检查点、保留 2；Agent 投影间隔 1；模型 Payload 记录开启。 |
| Brain | `book-case02-doorway-reading-brain`；完整 `SKILL.md` 位于 `experiment/skills/items/s0001/SKILL.md`。无子 Skill、对象被动 Skill 或评估器。 |
| 对话模型 | `Qwen3.8-27B-UD-Q4_K_XL`，`vllm`，`http://127.0.0.1:8888/v1`；temperature 0.2、max_tokens 2048、timeout 90 秒、retry_attempts 1、enable_thinking=false。 |
| 嵌入模型 | `openai_compatible`，model=`auto`，`http://127.0.0.1:5002/v1`；timeout 120 秒，传输与索引操作重试上限均 3。 |

精确 Brain 文件 SHA-256 为 `d1fae6b4c4fdcf8cbfd98b6b7c5035c40762b7cf1cf1a08d7443b9db5134f90f`，与 Skill Registry、实验完整性清单及 8 帧 Skill 执行记录一致。这里的哈希针对原始字节，不能用重新排版或转换换行后的文件代替。

另与本例 [skill/SKILL.md](../../skill/SKILL.md) 比对：**文字一致，原始字节不同，仅换行格式有差异**。本地文件为 4,927 字节、35 个 LF，SHA-256 为 `1a19f7edc1a2836f5709baa190edebfe4a6473fa781717e242dbbca6b1197933`；包内文件为 4,962 字节、35 个 CRLF。仅将包内 CRLF 统一为 LF 后，两者逐字节完全一致，无正文或结尾内容差异。本轮未覆盖任一 Skill 文件，精确运行字节仍以下载包为准。

模型配置记录的是本次使用的本地服务地址和凭据环境变量引用；模型服务及凭据仍需在复现环境准备，自包含实验文件不等于内置模型服务。

## 8 步世界事实

8 个帧文件连续、无缺步，Run/Attempt 身份一致。每步恰好有一条唯一世界事件，均含非空 `subject`、`predicate`、`object` 和非空 `structured_payload`；事件中的前后坐标、到达地址与该步 Agent 投影相符。

| Step | 虚拟时间（北京时间） | 已提交活动 | 位置及实际移动 |
| ---: | --- | --- | --- |
| 1 | 09:00 | 整理书包，确认书本、笔记本和笔 | `[7,7]`，门廊；ACT |
| 2 | 09:01 | 向阅读桌行进 | `[7,7] → [10,8]`，MOVE 4 格；剩余 6 格 |
| 3 | 09:02 | 继续穿过门洞前往阅读角 | `[10,8] → [14,8]`，MOVE 4 格；剩余 2 格 |
| 4 | 09:03 | 抵达阅读桌可走操作区 | `[14,8] → [16,8]`，MOVE 2 格；剩余路径为空 |
| 5 | 09:04 | 取出书本和笔记本 | `[16,8]`，阅读桌；ACT |
| 6 | 09:05 | 浏览书本章节 | `[16,8]`，阅读桌；ACT |
| 7 | 09:06 | 阅读书本一小节 | `[16,8]`，阅读桌；ACT |
| 8 | 09:07 | 在笔记本记录阅读摘记 | `[16,8]`，阅读桌；ACT |

三次 MOVE 合计 10 格，分配为 **4 + 4 + 2**。逐格与包内碰撞图核对通过，路径相邻格连续，没有穿越阻挡格或提前在目的地 ACT。导航本身没有代替 MOVE。包内 Step 7、8 的 `checkpoints/` 和 `recovery/` 共四套快照，成员大小、哈希和对应帧均一致。ZIP 未带入 Attempt 的可变工作记忆目录；已提交记忆保留在快照中。

## 调用与质量

模型 Trace 有 **33 条 LOGICAL_END、33 条 PHYSICAL_ATTEMPT、33 条 PHYSICAL_START**；33 个逻辑调用 ID 唯一，结束与物理尝试全部成功，物理 `attempt_no` 均为 1，即 **33 次逻辑调用 / 33 次物理尝试 / 0 次物理重试**。

按已提交 StepResult 内的 `mcp.call` 单独统计，实际 MCP 调用为 **36 次**：

| 工具 | 调用次数 | 拒绝或错误 |
| --- | ---: | ---: |
| `world-perceive` | 8 | 0 |
| `memory-stream-search` | 8 | 0 |
| `memory-stream-append` | 8 | 0 |
| `world-navigate` | 4 | 0 |
| `world-act` | 8 | 0 |

模型请求次数与 MCP 调用次数是不同统计口径，不能写成“只调用了 33 次工具”。8 次 `world-act` 均成功，对应 5 次 ACT、3 次 MOVE。

独立报告、ZIP 内报告和实际记录一致：仅 **Step 1 的 `EMPTY_MEMORY_RETRIEVAL`** 一项 WARNING，来源为首次检索 `query=""`、`limit=20` 返回空记录。后续检索有记录，没有 MCP 拒绝。质量状态为 `WARNING`，执行状态为 `COMPLETED`；不能把完成 8 步写成零观察项。

## 关于 `world-perceive` 半径参数的证据边界

此 ZIP 的模型 Payload 记录包含消息形式的 `request` 和模型 `response`，工具相关对象包含名称、调用参数和工具返回，**没有携带 `world-perceive` 的参数 schema**。因此，单凭本次下载包不能确认半径输入参数叫什么、是否列入当时下发的 schema。

能确认的事实是：精确 Brain 明确使用空参数对象，8 次实际 `world-perceive` 的输入均为 `{}`；观察结果含 `requested_radius_tiles`、`radius_tiles`、`vision_radius_tiles`，本次均为 10。**输出字段名不是输入参数 schema 的证据。** 本轮没有为确认此项再读取源码或调用接口。

## 普通 ZIP 与正式协议包

当前交付是**普通结果审计 ZIP**和独立质量报告。ZIP 内的实验自包含并通过实验协议完整性校验，但 ZIP 根目录没有 Run 的 `integrity/sha256.json`，本目录也没有独立 `.gaexp` 或 `.garun` 产物。

本次没有执行正式 Run 封存、异机导入或续跑验证，不能把此 ZIP 宣称为已验收的 `.garun`，不能仅改扩展名，也不能承诺一键恢复。其用途是保存本次实验定义、素材、已提交事实、检查点及审计证据。
