# 案例2对照：封闭门洞后的阅读桌可达性

**运行与MCP结果已读到，回放和导出验收被B02-007阻断。** 尚无本次下载ZIP，不能将此页称为完整导出核验。卡点及原文见 [阻断记录](../../blockers.md)。

| 项目 | 实际记录 |
| --- | --- |
| Experiment | `37378809-78d2-45c2-a4b6-5653f0f44142` |
| Run | `0f4f7968-bbd3-4812-aed9-acd3ccf6b6e7` |
| 状态 | COMPLETED，1/1，UI耗时26秒 |
| Brain | `book-case02-blocked-door-check`，公共ID`f39ed12c-2a6f-42df-9401-71817445b8ef`，UI内容哈希`9e497334105d`，3MCP、0脚本/子Skill |
| 模型调用 | 3逻辑/3物理，0重试，最大延迟11586毫秒 |
| Agent | 林晨，`agent-mttvoove-oxit70`，起点(7,7)，门廊三层地址，已知空间仅门廊空物件列表，视野10、注意力6、显示2.5格 |
| 参数 | 2026-09-09 09:00 Asia/Shanghai，1步×1分钟，seed42，检查点1/保留2/投影1，Payload开启，模型同正式案例 |

## 地图副本与真实请求

只将门洞(9,8)、(10,8)碰撞设为阻挡，保留背景及语义。编辑器两点检查(7,7)→(16,7)实际不可达；创建本实验导入副本后立即恢复公共地图，重查可达11格。完整操作见 [地图对照记录](../../../map/blocked-door-contrast.md)。

采用 [诊断Brain精确正文](../../../skill/contrasts/blocked-door-check.md)，当前目标通过UI明确改成原地检查通路，人物其他生活计划未改。Brain顺序为独立感知→等待真实阅读桌完整地址→独立导航→等待真实结果→原地ACT。

页面“仿真诊断→模型调用→最后一次物理请求”实际显示以下同Step MCP记录：

1. `world-perceive({})` 成功。实际半径10、attention6、未裁剪；`game_objects`中有阅读桌，距离9格，完整地址为`["林晨的门廊与阅读室","馆内阅读空间","阅读角","阅读桌"]`。
2. `world-navigate`只传上述原样完整`target_address`，未传坐标。`is_error=false`，真实返回`{"reachable":false,"reason":"BLOCKED_OR_DISCONNECTED"}`。这是合法查询的不可达结果，不是对象未感知、残缺地址或工具错误。
3. `world-act`提交一次ACT，predicate=`检查阅读通路`，object=`阅读桌的可达性`。实际`accepted=true`，自动补全`target_coord=[7,7]`和三层门廊地址，`path=[]`。没有MOVE、记忆读写或阅读活动。ACT摘要如实写未找到路径，不以其代替前两条真实返回。

质量页显示“行为质量：未评估”“基础诊断未发现告警；本次未执行业务评估”。这里只确认页面所述，不宣称独立质量JSON或包内报告已核验。

## 未完成项与停点

已确认“感知到阅读桌”和“阅读桌可达”可以产生不同结果；对照符合预期空间边界。随后回放资源加载明确超时，按用户规则停止，登记B02-007并等待评估。停止操作、保留页面时，异步提示“制品任务`7d2a3e65`已完成”；未继续刷新下载或核验，不能说导出后端失败，也不能称已归档。还需完整回放核验、最终原始下载、两格碰撞差异及包完整性验证。未检查真实运行目录，也没有通过接口或脚本补造结果包。
