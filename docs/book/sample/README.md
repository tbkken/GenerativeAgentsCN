# 书稿案例素材

每个案例都是独立目录，固定包含 `map/`、`agents/`、`skill/` 和 `settings/`，不依赖另一个案例的文件。

- [案例 1：陈明远醒来后的晨间工作启动](01-morning-routine/README.md)

案例目录中的 JSON 既区分严格的 Studio 作者字段、实验 placement 和运行设置，也明确标注哪些文件是上传/录入参考而不是一键导入接口。

