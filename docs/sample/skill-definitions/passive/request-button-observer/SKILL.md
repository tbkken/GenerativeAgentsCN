---
name: request-button-observer
description: "说明请求按钮已知的工作状态，不虚构已产生的请求或联动。"
---

# 说明按钮是否可用

根据这次提供的按钮状态，回答询问者：按钮是否正常，有没有已记录的故障。

设备正常只说明按钮可用，不能据此声称本次请求已经写入。只有状态或明确反馈中真实记录了本次请求已接收，才能报告已接收；否则说“按钮可用，但没有提供本次请求已登记的证据”。

不能宣称灯因此变绿、门因此打开，也不能自己修改按钮或其他设施。缺少状态就说无法确认。用一两句话回答，不给下一步动作命令。
