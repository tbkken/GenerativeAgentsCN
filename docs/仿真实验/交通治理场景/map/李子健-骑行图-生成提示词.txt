工具：内置 image_gen.imagegen。两次输出均未通过尺寸与透明通道检查，未上传系统。

v1，参考图仅用于风格与视角：交通治理-核心街区背景-v3-实验路线.png

Use case: stylized-concept. Generate ONE production game sprite sheet for the traffic-governance experiment, character Li Zijian (李子健), a 28-year-old Chinese male office commuter riding a compact electric bicycle. Input image is STYLE AND VIEWPOINT REFERENCE ONLY; do not change or reproduce the map. Match its softly painted detailed top-down 2.5D Chinese town game style. Character wears a muted teal office shirt, dark trousers, simple helmet and small dark backpack; a compact dark electric bicycle with two clearly readable wheels, hands on handlebars, no logos. Required deliverable: transparent PNG sprite sheet, EXACTLY 128 by 128 pixels for the game uploader (not merely a larger image described as 128). 4 columns × 4 rows, every cell exactly32×32, grid-aligned with zero outer margins and no grid lines. Each of 16 cells contains the SAME character and same bike, entirely inside its own cell with 2px safety margins, anchor centered horizontally and wheel contact near bottom at same height. Four animation frames per row with subtle cycling changes, no changing outfit or bike. Row1 faces DOWN/south, row2 LEFT/west, row3 RIGHT/east, row4 UP/north. Orthographic overhead RPG game view; all bike and rider parts must fit in each cell. True alpha transparency, no opaque background, no shadow puddles extending beyond cell, no text, no labels, no scenery. Please finish by exporting the actual final file at 128×128 so it can be uploaded directly.

v2，编辑目标：李子健-骑行图-v1-生成原稿.png

Precise production export correction. This target sprite sheet has a FAKE CHECKERBOARD physically painted in RGB pixels. Remove that entire checkerboard and replace every background pixel with TRUE ALPHA TRANSPARENCY. Do not paint any checkerboard, gray, white, colored background, halo, ground shadow, or label. Preserve the same 16 rider+bicycle figures, exact 4-column by4-row layout and down-left-right-up direction order, identity, pose and colors. Export a genuine RGBA PNG with an alpha channel. Final file MUST be exactly128×128pixels, each cell32×32pixels; please perform the final export at that actual file size, not a high-resolution mockup. This is a game texture, not a presentation of a texture. No border, no scene, no typography. Return the usable transparent128×128PNG file.
