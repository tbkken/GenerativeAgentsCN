# MOVE 活动语义修复与原 Run 验收

状态：**工程修复及原页面复验通过；实验继续验收暂停在 5/60，导航目标误用单独待处理。** 本次没有更改已封存 Brain、Agent、地图或摄像头 Skill，也没有创建替代 Run。

## 设计边界

保留内核核验的 `Event(Agent, 移动到, 实际落点地址)`、真实位移、路径及地址一致性。复用 MCP 既有 `predicate` 输入，在实际 MOVE 成功提交后保存正式 `movement_activity = {text, source: actor_declared}`。活动经同一次 World Commit 进入 Event、当前状态、StepResult、感知及回放；不由原始模型文本或 `requested_*` 驱动画面，不给历史帧补写活动。

没有新增交通活动枚举、活动字典、业务排程或用户脚本要求；摄像头如何根据可见活动和轨迹判断仍由自然语言 Skill 决定。未填写活动时不擅自推断步行，缺省图标为中性箭头。详细合同见仓库 `docs/move-activity-contract.md`。

## 工程验证与重启

工程子 Agent `fix_move_activity_semantics` 完成修复，报告 **101 passed, 1 warning**。警告为现有 FastAPI TestClient/httpx 弃用提醒。新增回归穿过真实 MCP → Commit → StepResult → 普通 Agent/对象感知 → 序列化恢复 → 两种 Replay 投影，包含步行、推行、骑行、未提供活动、视野裁剪和实际落点与自由文本冲突。没有调用模型来预设抓拍结果。

执行命令：

```powershell
$env:PYTHONUTF8='1'
python -m pytest tests/runtime/test_move_activity_semantics.py tests/runtime/test_world_commit_event_sync.py tests/runtime/test_brain_capability_runtime.py tests/runtime/test_object_skill_runtime.py tests/runtime/test_visible_object_navigation.py tests/foundation/test_navigation.py tests/runtime/test_action_timezone.py tests/runtime/test_replay_world_state.py tests/architecture/test_portable_module_boundaries.py -q
```

主 Agent 审阅了提交边界、活动验证、感知裁剪、协议传递和定向测试。浏览器先确认原 Run 暂停在 Step 3，进程检查仅发现 Web，无活动仿真 worker。然后通过已授权的 `restart-web.bat` 隐藏窗口重启：旧 Web PID `52016` → 新 PID `61096`。重启日志 `var/logs/restart-move-activity-20260913.log` 返回成功，健康检查为 `status=ok`、`runtime_truth=files`、`database_owner=ga_studio`。

## 原页面恢复与事实核验

- Experiment：`6092db14-4f63-4986-8dd8-b5c81b07ffca`，交通治理·C1-L·李子健。
- Run：`3cb1414a-be8e-4d7b-bace-38f4e3b8d540`，恢复前后保持同一 ID。
- 原 Attempt 01：`60716c61-50e7-41eb-9053-60a986078a8a`，提交 Step 1–3，PAUSED。
- 新 Attempt 02：`6e715d66-2007-4416-bb8d-9040975fd9aa`，提交 Step 4–5，PAUSED。
- 入口：原实验 → 实验结果 → 仿真回放。整页刷新后仍能查看原 Step 1 的旧事实。点击“继续执行 · Step 3”，弹窗显示有效恢复点 Step 3、继续位置 Step 4、保持原 Run 并创建新 Attempt，确认后恢复成功。

浏览器“查看事实详情”核对结果：

| 项目 | Step 4 | Step 5 |
|---|---|---|
| 虚拟时间 | 2026-01-01 08:03 +08:00 | 2026-01-01 08:04 +08:00 |
| 实际位移 | [11,26] → [12,26] | [12,26] → [13,26] |
| 已执行路径 | [[11,26],[12,26]] | [[12,26],[13,26]] |
| 正式 predicate | 移动到 | 移动到 |
| movement_activity.text | 骑电动自行车前行 | 骑电动自行车前行 |
| movement_activity.source | actor_declared | actor_declared |
| emoji | ➡️ | ➡️ |
| reached_target | true | true |

两步实际落点均属于“新港西路交通治理·核心街区 / 新港西路沿街街区 / 南侧东行非机动车道 / 西段骑行出发区”。`reached_target=true` 只指抵达当次明确提交的相邻目标，不能解释成已抵达办公入口或完成通勤。

Agent 检查器的动作、当前状态和地图气泡均显示“移动活动：骑电动自行车前行”，骑行人物素材可见。Step 5 的模型调用序号 20（10096 ms）请求体中，`variables.currently` 与 `variables.current_action.movement_activity` 同时保留 Step 4 的活动，确认下一轮上下文没有退化为通用移动。近景气泡内容仍受视口宽度裁剪；本次依据完整检查器和事实详情核对文字，没有把近景截图当作全部视觉验收通过。

模型仍将导航的第一格当作 MOVE 目标。主 Agent 在 Step 4 后通过 UI 请求安全暂停，最终浏览器显示“仿真 1 · 已暂停 · 5/60 步”和“继续执行 · Step 5”。文件事实交叉核验暂停提交时间为 `2026-09-13T13:33:46.160257Z`，即北京时间 `2026-09-13T21:33:46.160257+08:00`。这个待处理的模型行为见 [独立记录](导航目标误用-修复后复验-2026-09-13.md)，没有再修改系统或已封存 Skill。

## 历史保留及验收范围

修复前保存 20 个文件的 SHA256：Run Manifest、前三帧和完整内嵌实验文件。重启后、恢复提交 Step 4–5 后分别核对，**20 个文件全部一致，0 个改写**。详见 `MOVE语义修复-验收基线.json` 与 `MOVE语义修复-历史保留校验.json`。新步骤接续 Step 3 的实际坐标，没有重跑前 3 步。

正式活动进入摄像头感知的链路由工程回归覆盖；真实 Run 中人物仍在摄像头视野之外，摄像头等待下一轮，尚未验证真实监测区内的抓拍或不抓拍决策。此次通过的是 MOVE 活动语义修复，不是完整通勤、时间压力校准、六组比较或整个交通治理实验验收。
