# 首份交通治理运行验收

Experiment：`6092db14-4f63-4986-8dd8-b5c81b07ffca`，名称“交通治理·C1-L·李子健”。Run：`3cb1414a-be8e-4d7b-bace-38f4e3b8d540`。全部系统配置、保存、刷新核验、地图校验与启动均由主 Agent 通过浏览器完成。

当前状态：**用户明确要求继续实验，已通过浏览器从 Step 5 恢复同一 Run；当前 Attempt 03 为 db473f27-110f-416e-99ec-b075aa02a4dd。** MOVE 修复已通过原 Run 复验，导航目标误用继续观察，不再作为暂停条件。最新进展和后续配置见 [继续运行记录](继续运行与新增配置-2026-09-13.md)。以下启动、暂停和修复过程为历史证据，不代表当前 Run 已暂停。

## 本次已配置与核验

- 李子健出生点 `(8,26)`。地图实际返回并同步的四层地址为“新港西路交通治理·核心街区 > 新港西路沿街街区 > 南侧东行非机动车道 > 西段骑行出发区”。原自动分配的 `(0,0)` 校园起点已通过 UI 修改；已知空间同步为南侧东行非机动车道，已知物件仅为西段骑行出发区。保存后整页刷新重开，坐标、地址与已知空间一致。
- 地图显示大小 2 格，视野 20，注意力 8；两张 128×128 图片与 4x4 布局仍在。实验内场景人物比例待首次回放核验。
- 原实验地图独立副本为 120×40、22 个节点，包含完整背景与摄像头图片。点击“校验地图”，页面返回“地图校验通过”。
- 实验内 Brain 为 `traffic-rider-brain`，UI 内容标识 `74536ad090e7`，正文与本地 `skill/traffic-rider-brain/SKILL.md` 一致；依赖页列出 world-perceive、world-navigate、world-act、memory-stream-search、memory-stream-append，0 个子 Skill、0 个私有脚本。
- 模型页显示聊天模型 `Qwen3.8-27B-UD-Q4_K_XL`，地址 `http://127.0.0.1:8888/v1`；Embedding 为 `auto`，地址 `http://127.0.0.1:5002/v1`。只核对已复制配置，未改模型服务或密钥。
- 起始时间 `2026-01-01T08:00:00+08:00`，Asia/Shanghai，每步 1 分钟，共 60 步；seed 42，检查点每步 1 次、保留 2 份，Agent 状态每步投影，开启模型 Payload 审计。参数已保存，后续整页重载及概览再次确认。
- 当前目标中显式写入 task_deadline `2026-01-01T08:50:00+08:00` 和观察结束 `2026-01-01T09:00:00+08:00`。完成顺序依赖事实进度：骑行到达、停稳并下车、报到、有限等待；没有按 Step 编排行为。完整原文保存在 `首份实验草稿-6092db14.json` 的 `agent_ui.currently`，仅修改实验内角色副本，公共角色保持通用任务文本。

本次截止时间与 60 分钟窗口用于先核验完整运行，尚未按实际移动预算计算 K_ref，不作为六组正式比较的已定稿 L/H 参数。当前无额外背景交通，不声称拥堵、违规借道更快或预设违规结果。72 次正式出行没有启动。

## 执行前页面证据

确认弹窗显示 1 Agent × 60 steps / SKILL Brain；0 个阻断项、0 个警告、0 项自动检查、1 项通过。预计模型调用 120–360，Token 60,000–360,000，墙钟 4 分钟–1 小时，存储 240 KB–3.8 MB。这些只是 UI 估算，不是实测。

确认执行后页面显示“实验已执行”，创建上述 Run，实验生命周期变为已封存，Run 选择器最初为“仿真 1 · 排队中 · 0/60 步”。诊断页最初为“尚未创建执行尝试”。没有将创建成功当作模型、摄像头、移动或抓拍验收通过。

## 实际启动与阻断

Attempt 01 于页面日志时间 2026-09-13 19:57:36（Asia/Shanghai）显示 1 Agent 和 1 Game Object 的 Skill runtime 就绪。首步模型上下文正确包含起点、四层地址、1 分钟步长、60 步、人物背景和任务截止时间；真实 world-perceive 请求/生效/硬上限半径均 20，注意力 8。导航 [28,26] 可达，返回距离 20、下一格 [9,26]；模型实际请求并提交 MOVE 到 [9,26]。摄像头首步产生等待下一轮的世界事件，此时人物尚不在摄像头视野内，不构成抓拍验收。

在首步回放事实中确认 MOVE 的 requested_predicate 为“骑电动自行车前行”，正式 predicate 为“移动到”，当前状态和主描述同步变成通用移动，图标为步行。主 Agent 按约定阻断，2/60 时提交暂停，浏览器随后确认“仿真 1 · 已暂停 · 3/60 步”。没有修复代码、调整已封存 Skill 或创建替代 Run。首步模型选用 next_coord 的行为偏离单独记录，不把它写成系统移动预算错误。


## 修复后的原 Run 续跑

2026-09-13 主 Agent 在 101 项定向测试通过后重启 Web（PID 61096），通过原页面从 Step 3 恢复至新 Attempt 02。Step 4–5 的正式 movement_activity、当前状态、回放和下一轮 Brain 上下文均保留“骑电动自行车前行”，真实路径依次为 [11,26]→[12,26]、[12,26]→[13,26]。原前三帧及内嵌实验共 20 个文件哈希全部一致。由于模型仍将导航第一格选作 MOVE 目标，UI 再次安全暂停于 Step 5；没有修改已封存 Skill 或继续运行挑选成功结果。真实监测区内摄像头判定仍未验收。
