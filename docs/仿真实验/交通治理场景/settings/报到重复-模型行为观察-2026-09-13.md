# 报到重复：当前模型行为观察

状态：已核对完整边界，Step33～35连续报到，Step36～37自然转入有边界的WAIT；没有修改代码或Skill，也未定案为系统缺陷。按用户“继续实验，不要停”的指令，原Run保持执行，没有暂停、改写封存Brain或回填历史事实。

- Experiment：6092db14-4f63-4986-8dd8-b5c81b07ffca，交通治理·C1-L·李子健。
- Run：3cb1414a-be8e-4d7b-bace-38f4e3b8d540。
- Attempt03：db473f27-110f-416e-99ec-b075aa02a4dd。
- 浏览器入口：实验结果 → 仿真回放 → Step33～35；仿真诊断 → 模型调用 → Attempt03。

## 已核对证据

Step31：MOVE从(104,26)经(104,27)抵达(104,28)，reached_target=true，remaining_path为空；四层地址为东端办公报到入口。

Step32：模型向该完整对象地址查询导航，返回reachable=true、distance_tiles=0、movement_required=false；随后成功提交独立ACT，predicate为“停稳车辆并下车”。调用审计call_id为56c0f39f-e088-4871-acc3-25e0b10d92d5。

Step33：模型再次确认目的地零距离，成功提交ACT“办理报到”，该动作出现在Step33回放事件中。审计call_id为47ff7198-6500-4b91-8abe-8d92364688ec。

Step34～35：位置保持(104,28)，继续出现相同“办理报到”动作，没有切换到观察结束前的有限WAIT。Step34事实详情明确action_type=ACT、predicate=办理报到、executed_path=[]；不是用MOVE描述冒充到达，也不是暂停恢复重复提交同一Step。

Step35回放的记忆变化显示，真实读取到了Step34和Step33的最新进度便签。Step34便签已写“上轮‘办理报到’已作为当前动作记录”，但随后又拟提交同一ACT；Step35新便签重复相同判断。因此当前证据不能解释为记忆丢失或只检索到早期移动记录。

## 当前判断与验收边界

现有证据指向Brain/模型没有把上一轮报到事实明确归入“本次任务已完成”，而持续把它解释为仍需办理。自然语言活动是否已经完成需要实验Skill约定；不能为此在内核增加“报到一次后必须WAIT”的活动字典或硬编码流程。

这还不能单凭三步断言运行卡死，也不把“办理报到”文字直接统计为一次已完成任务。截至Step35时抵达与下车已核验，有限等待尚未出现；随后Step36～37已有正式WAIT，更新证据见下节。需要后续完整Step序列和质量结果确认重复范围，再决定是否在新的独立实验中加强Brain的任务完成确认。没有修改当前已封存Run，也没有静默给所有草稿替换Brain。

已配置草稿均使用同一现有Brain，可能出现同类进度问题；预检通过不保证不会出现同类多步报到；本次后续自然结束重复，也不能抹掉已有三步记录。


## 后续自然转入等待（未修改运行）

Step36（08:35）正式WAIT，SPO为李子健／等待／下一轮，位置(104,28)、executed_path为空，expected_until_step=37，expected_until_time=2026-01-01T08:36:00+08:00；wait_reason明确报到已完成、车辆已停稳并下车、不再移动，并说明下一轮核对09:00观察边界。

Step37（08:36）继续正式WAIT，expected_until_step=38、expected_until_time=2026-01-01T08:37:00+08:00，仍在目的地。两步均已展开结构化事实详情验证，未仅根据描述认定WAIT。

因此不能把Step33～35的重复直接定性为永久卡死。当前行为链已真实经过抵达、下车、报到和有限等待；三步报到作为实际任务活动跨度保留，在后续评估中说明，不把第一次ACT自动等同于已完成，也不删除中间步骤。
