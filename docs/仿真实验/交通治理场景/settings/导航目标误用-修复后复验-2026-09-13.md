# 导航目标误用：接口澄清后仍需处理的模型行为

状态：**用户已明确要求继续实验，不再因本项暂停。** 2026-09-13 用户指示“继续实验，不要停，然后往后继续配置”；主 Agent 已通过原页面从 Step 5 恢复，保留本项为已知模型行为观察。下文为发现时的证据与建议，不再作为当前运行阻断，也不表示修改 Brain 或内核方案已获确认。

## 入口与身份

原实验 `6092db14-4f63-4986-8dd8-b5c81b07ffca` → 实验结果 → 仿真诊断 → 模型调用 → Attempt 02。Run `3cb1414a-be8e-4d7b-bace-38f4e3b8d540`；Attempt `6e715d66-2007-4416-bb8d-9040975fd9aa`；Agent `agent-mtzlbf8j-5yfte2`（李子健）。Step 4 发现本次复验仍出现相同模式，UI 安全暂停于 Step 5。

## 原始请求和结果

Step 4 模型调用序号 5（18043 ms）请求：

```json
{"name":"world-navigate","arguments":{"target_coord":[28,26]}}
```

下一次模型调用序号 8（27848 ms）的请求体，已收到真实工具返回：

```json
{
  "reachable": true,
  "distance_tiles": 17,
  "next_coord": [12,26],
  "movement_required": true,
  "requested_target": {"target_coord":[28,26]},
  "next_coord_role": "FIRST_PATH_TILE_NOT_DESTINATION"
}
```

同次模型响应却写入进度便签：“本轮拟提交 MOVE 至 [12,26]”。随后序号 11（10924 ms）的真实调用是：

```json
{
  "name":"world-act",
  "arguments":{
    "action_type":"MOVE",
    "target_coord":[12,26],
    "predicate":"骑电动自行车前行",
    "object":"南侧东行非机动车道",
    "description":"沿南侧东行非机动车道继续向东骑行，朝东端办公报到入口方向前进。"
  }
}
```

正式 Step 4 因而只走 `[11,26] → [12,26]`，Step 5 再走 `[12,26] → [13,26]`。各次实际落点、路径和收到的相邻目标一致，修复后的骑行活动也已保留。

## 预期、影响及诊断边界

Brain 已要求把同一导航目标用于 MOVE；接口也明确第一格不是目的地。若决定继续前往 `[28,26]`，应把 `[28,26]` 作为 MOVE 目标，允许内核按本步预算提交实际可走路段；模型本次没有遵守这一约定。系统不能暗中替换其明确提交的 `[12,26]`，否则会违反 Skill 决定调用与目标、内核校验执行的边界。

当前不能用这种每步 1 格的结果校准移动预算或 L/H 时间压力。若持续如此，会扭曲通勤成本，甚至让未到目的地变成导航误用的产物，失去交通治理对照意义。摄像头实际判定也尚未进入验收范围。

旧记忆里存在连续“导航远端、MOVE 下一格”的便签，可能强化了该模式；这是待验证的影响因素，不能据此断言已找到模型内部决策原因。接口措辞澄清已实际送达，但对本次原 Run 不足以改变行为。没有把单次模型行为上升为确定的内核缺陷。

建议下一步在用户确认后，通过 Studio 修改公共 Brain 的自然语言 SOP，用目标变量及一个简洁例子明确“路径第一格仅供理解，MOVE 使用本次选定目的地”；按 SEALED 生命周期经 UI 建立独立实验并明确导入更新的 Brain，保留当前 Run 作为失败证据。不能在当前 Run 内替换 Brain 或清空记忆，也不能由系统硬编码远端目标、步数排程或交通方式。

验收条件：核对实际 world-navigate 与 world-act 输入使用同一明确目标；部分路径按真实预算提交；下一轮依据实际落点继续；到达前不执行目的地活动。保留失败运行，分别报告活动语义修复与导航行为结果，不靠多次重跑挑成功样本。
