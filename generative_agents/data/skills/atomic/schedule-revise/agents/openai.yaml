interface:
  display_name: "Schedule Revise"
  short_description: "Run the Schedule Revise agent skill"
  default_prompt: "Use $schedule-revise to handle the current agent context and return the result directly."
