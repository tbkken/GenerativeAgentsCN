interface:
  display_name: "Schedule Daily"
  short_description: "Run the Schedule Daily agent skill"
  default_prompt: "Use $schedule-daily to handle the current agent context and return the result directly."
